# 카카오톡 테마

카카오톡 테마 모음을 만들어 배포하는 저장소다. iOS `.ktheme` 과 안드로이드 `.apk` 를 같이 낸다.
저장소: https://github.com/Ruminem/kakaotalk-theme (public)

## 원본은 하나뿐이다

색과 테마 정의는 `tools/themes.py` 팔레트 표에만 있다.
CSS, `colors.xml`, `AndroidManifest.xml`, 말풍선 그림, 미리보기가 전부 거기서 생성된다.

```
tools/themes.py     ← 팔레트 표. 여기만 고친다
tools/gen.py        → build-src/<테마>/{ios,android}/
tools/preview.py    → assets/*.webp, docs/index.html, docs/themes/*.md, README 의 테마 목록
build.ps1           → dist/iOS/*.ktheme, dist/android/*.apk
release.ps1         → 태그 + 릴리스 자산 첨부
.github/workflows/pages.yml → ruminem.github.io/kakaotalk-theme/
```

`docs/` 안에서 **`index.html` · `themes/*.md` · `themes.json` 은 생성물이고
`share.html` 과 `make.html` 은 손으로 쓴 것**이다.
같은 폴더에 있지만 성격이 반대다 — `index.html` 을 고치면 다음 미리보기에서 날아간다.

**README 와 갤러리 순서는 카테고리 순 → 그 안에서 팔레트 표 순이다.** 카테고리는 `CATEGORIES` 가
정하고, 그 안에서는 팔레트 표에 쓴 차례를 그대로 따른다. 변형(v2)은 `_add_variant` 로
원본 바로 뒤에 끼워 넣는다. 뒤에 몰아 붙이면 원본과 떨어져서 무엇을 고친 건지 안 보인다.

**테마를 하나 더할 때는 `quartet()` 으로 네 벌을 함께 만든다.**

| | 배경 없음 | 배경 있음 |
|---|---|---|
| **글로우 없음** | `기본` | `배경` |
| **글로우** | `글로우` | `글로우+배경` |

**이 네 이름표가 폰의 테마 목록에도 그대로 붙는다** — `야경 기본`, `야경 배경`,
`야경 글로우`, `야경 글로우+배경`. 넷을 나란히 깔았을 때 목록에서 바로 구분돼야 한다.
이름표는 `TYPES` 에 있고 계열 이름 뒤에 붙는다.

색을 한 벌 정하면 네 가지 인상이 나온다. 배경 없는 쪽은 담백하고, 배경 있는 쪽은
분위기가 있고, 글로우는 같은 색이 떠 보인다. 고르는 사람마다 원하는 게 달라서
하나만 내면 절반은 아쉬워한다. 네 벌이 한 계열이 되고 README 에서 2x2 로 묶인다.

### 이름이 세 가지다

| 쓰는 곳 | 모양 | 나오는 곳 |
|---|---|---|
| 배포 파일 | `city-glow-image` | `city-glow-image.ktheme` / `.apk`, README 링크 |
| 패키지 · iOS 테마 ID | `city_glow_image` | `com.kakao.talk.theme.city_glow_image` |
| 미리보기 그림 | `preview-city-glow-image-chat.webp` | `assets/`, README |
| 키 | `city21` | 겉으로 안 드러난다. `versionCode` 번호로만 쓴다 |

파일 이름은 하이픈, 패키지는 밑줄이다. **안드로이드 패키지 이름은 자바 식별자 규칙이라
하이픈을 못 쓴다.** 둘 다 `file_slug()` 하나에서 나오므로 따로 관리하지 않는다.

변형 이름을 `VARIANT_SLUG` 에 등록하지 않으면 생성이 멈춘다 — 파일 이름을 지어낼 수 없어서다.

키의 번호는 `versionCode` 에 쓰이므로 겹치면 안 된다. 이미 나간 테마를 네 벌 중 하나로
편입할 때는 `keys` 로 그 키를 그대로 쓴다 — 번호가 바뀌면 `versionCode` 가 내려갈 수 있다.
야경이 그렇게 편입됐다.

**패키지 이름이나 iOS 테마 ID 를 바꾸면 이미 깐 테마와 별개가 된다.** 폰에는 둘 다 남고
업데이트로 이어지지 않는다. 바꿀 때는 릴리스 노트에 적어서 지우고 다시 깔게 한다.

네 벌 모두 아래 규칙을 따른다. `standard()` 가 검사하고 어기면 멈춘다.

1. 말풍선에 그라데이션 없음 — 단색
2. 말풍선 네 칸이 서로 다른 색
3. 배경을 넣으면 세 화면(목록·채팅방·잠금화면)에 다 넣는다. 한 곳만 넣지 않는다
4. 아이콘은 자동 생성이라 따로 할 일 없음

`quartet()` 은 색부터 새로 정할 때 쓰고, 이미 나간 테마에 형제를 붙일 때는
`fill_family()` 를 쓴다. 바탕 테마의 말풍선 성격(그라데이션이냐 단색이냐)을 그대로
물려받는다 — 계열 안에서 말풍선 재질이 제각각이면 같은 계열로 안 보인다.

**이름표와 실제 내용이 어긋나면 안 된다.** `배경` 은 그림만, `글로우` 는 빛만,
`글로우+배경` 이 둘 다다. 심야 `배경` 이 글로우를 달고 있어서 `글로우+배경` 과
사실상 같은 테마였던 적이 있다 — 배경 그림까지 한 글자도 안 틀리고 같았다.
파일이 둘인데 화면이 같으면 왜 둘인지 알 수 없다. `check_variants()` 가
불러올 때마다 검사하고, 내용이 겹치는 변형이 있으면 멈춘다.

**배경 그림은 테마마다 다르게 나와야 한다.** 장면 함수에 씨앗을 하나 박아두면
같은 장면을 쓰는 테마가 전부 같은 그림이 된다. 씨앗은 `seeded()` 가 키에서 만든다 —
테마마다 다르고, 같은 테마를 다시 돌리면 같은 그림이 나온다.

**유리 계열은 예외다.** 리퀴드 글래스·프리즘 글래스·프로스트 글래스는 라이트/다크 × 배경/단색,
원목 글래스는 오크/월넛 × 배경/단색, 스테인드 글래스는 색 네 개가 곧 네 벌이다. `배경 그림 / 글로우` 축이 아니라
`밝기 / 배경 유무` 축으로 넷을 만든다. 뒤에 더한 씨글래스·글래스 블록·얼음·골판 유리·아크릴·레진은 같은 축을
`character()` 의 밝음/어두움 × 배경으로 만든다(아래 "유리 카테고리의 새 계열"). 글로우도 안 넣는다 — 반투명 말풍선은
테두리로 이미 떠 보이고, 원목처럼 난색 중명도 바탕에서는 빛이 번질 자리가 없어
글로우가 빛이 아니라 뿌연 테두리로 보인다.

**차분과 고요도 예외다.** 색 네 개가 곧 네 벌이고 글로우와 배경 그림을 아예 안 쓴다 —
둘 다 이 계열의 목적과 정면으로 어긋난다. 눈이 편하다는 건 취향 같지만 숫자로 꽤 잡힌다.

| 값 | 기준 | 비교 |
|---|---|---|
| 채도 | 0.25 아래 | 레드 1.00, 캔디 팝 0.60, 크림 라떼 0.47 |
| 바탕↔말풍선 대비 | 1.2 ~ 2.0 | 말풍선이 구분되면서 경계가 안 튀는 구간 |
| 글자 대비 | 4.5 이상 | 읽히는 최소선 |
| 순백·순흑 | 안 쓴다 | `#FFF` 와 `#000` 은 그 자체로 눈이 아프다 |

색을 고르고 나서 눈으로만 보면 "예쁘다"에 끌려 채도가 올라간다. 먼저 재고 나서 그린다.
**포인트색도 잰다.** 말풍선만 재면 놓친다 — 고요 올리브는 말풍선이 전부 기준 안이었는데
포인트색 하나(`#7D7A4E`)가 채도 0.38 이었다. 알림 숫자와 전송 단추가 그 색이라 화면에서 제일 튄다.

유리는 재질이 축이라 계열이 많다. **색만 다른 유리는 더 만들지 않는다** —
스테인드가 그 자리를 이미 네 칸 쓴다. 새 유리를 만들려면 재질이 달라야 한다(테두리, 알갱이, 배경).

**캐릭터 계열도 예외다 — 그리고 새 계열은 이제 이쪽이 기본이다.** 우체국·책상·오락실·빨래·
영화관·빵집·캠핑·우주·온실은
`character()` 로 `밝음` · `밝음+배경` · `어두움` · `어두움+배경` 네 벌을 만든다.
모양 있는 말풍선은 그 자체로 눈에 띄어서, 고르는 사람이 궁금한 것은 글로우를 얹느냐가 아니라
밝은 방에서 쓰느냐 어두운 방에서 쓰느냐다. `check_variants()` 가 이름표의 밝음/어두움을
바탕 밝기와, `+배경` 을 그림 유무와 대조한다.

**글로우는 어울리는 계열에만, 네 벌 모두에 넣는다**. 네온 느낌인 오락실과
모닥불이 있는 캠핑이 쓴다. 종이(편지·포스트잇)에 빛을 두르면 빛이 아니라 번진 테두리로 보인다.
사용자가 따로 원하는 계열에는 넣는다. 이미 나간 계열은 기존 축 그대로 둔다 — 파일 이름이 바뀌면
깐 테마와 끊긴다.
**빛의 출처가 따로 있으면 그 색을 준다**(`char_glow_color`). 기본은 말풍선 색이라 갈색 팻말에서는
갈색 빛이 번져 얼룩이 됐다. 모닥불 주황을 주니 불빛 받은 나무 모서리로 읽혔다.

**화면 속 빛에는 광택을 두르지 않는다**(`glow_edge='phosphor'`). 옛 글로우(`quartet()` 의 네 벌 중
글로우 둘)는 가장자리에 흰 쪽으로 밝힌 띠와 흰 반사선을 두른다. 터미널의 검은 초록 칸에서는 그게
빛이 아니라 회백색 테두리로 떠서 사용자가 "너무 짜친다" 고 했다. 모니터에는 광택이 없다 — 어두운 칸은
포인트색 가는 선과 안쪽으로 옅게 번지는 빛을 두르고 바깥 번짐도 포인트색으로 하고, 밝은 칸은 몸통이
곧 빛이라 가장자리를 칠하지 않는다. `glow_of()` 가 `glow_edge` 를 적은 테마만 칸을 더 얹어 넘기므로
다른 옛 글로우 테마 37벌은 그림이 그대로다(0.31.1 에서 해시로 확인). 같은 흰 테두리가 다른 계열에서도
거슬리면 그 계열에만 `glow_edge` 를 적는다.

### 글로우 모양

**글로우는 손으로 짜지 않고 `tools/glow.py` 의 사전에서 이름으로 고른다.** 테마마다 짜면 계열마다
빛이 제각각이 되고 같은 실수(네모 잘림, 간격 벌어짐)를 되풀이한다. 새 캐릭터 말풍선 테마를 더할 때는
`material` 을 적으면 아래 표의 글로우가 붙는다. 다른 모양을 원하면 `glow_style` 로 덮어쓴다.

| 이름 | 모양 | 여백 | 기본으로 쓰는 재질 |
|---|---|---|---|
| `char` | 두 겹 흐림 | `char_glow` | 0.29~0.30 에 나간 오락실·캠핑. **고치지 않는다** |
| `soft` | 은은한 번짐 | 8 | `light` 그 밖에 스스로 빛나는 것 |
| `bloom` | 관 가까이가 하얗게 타고 멀리 퍼짐 | 13 | `tube` 네온관 |
| `led` | 짧고 또렷함 | 4 | `pixel` 픽셀 |
| `wall` | 뒤 벽에 넓고 옅게 번짐 | 18 | — |
| `drop` | 아래 벽으로 흘러내림 | 아래 18 | `sign` 벽에 매단 간판 |
| `ring` | 바깥에 옅은 테가 한 겹 더 | 13 | — (폰 크기에선 옅다) |
| `shift` | 멀어질수록 옆 색으로 번짐 | 12 | `party` 파티·사탕 |

`paper`·`glass` 재질에 글로우를 넣으면 생성이 멈춘다. 여백이 `MAX_MARGIN`(18pt)을 넘어도 멈춘다 —
글로우 여백을 카톡이 말풍선 칸으로 쳐서, 넓게 번질수록 연달아 보낸 말풍선 사이가 벌어진다.

**어떤 모양이 어울릴지는 그려 보고 고른다.** `python tools/glowsheet.py <계열 이름>` 이 모든 모양을
그 테마의 말풍선·배경으로 한 장에 그린다(`build-tmp/`, 저장소 밖). 넓은 모양은 여백을 키우지
않으면 흐림이 장 끝에서 잘려 말풍선 둘레에 네모가 떴다 — 사전의 새 모양은 전부 가장자리에서
0 으로 사그라든다. 점박이·줄무늬처럼 한 줄씩 무늬가 있는 빛은 긴 말에서 늘어날 때 뭉개져서 넣지 않는다.

**`char` 는 옛 계산을 그대로 옮긴 것이다.** 이미 깐 테마의 그림이 달라지면 안 되므로, 사전으로
옮긴 뒤 140개 테마의 말풍선·CSS·프로필을 해시로 대조해 달라진 것이 없음을 확인했다.
기존 테마를 새 모양으로 바꾸고 싶으면 그 계열만 따로, 릴리스 노트에 적고 한다.

**배경에 광원이 있는 변형만 말풍선도 그 빛에 비치게 한다**(`firelight`, `character(..., extra=)`).
캠핑 어두움+배경은 불이 화면 아래 가운데에 있어서, 화면 가운데를 향한 안쪽 아래 귀퉁이에서
빛이 들어와 윤곽선을 따라 아래변으로 길게, 옆변으로 짧게 번지다 사그라든다. 배경이 없는 어두움에는
넣지 않는다 — 빛의 출처가 화면에 없으면 까닭 없는 얼룩이다.

**빛은 가까운 곳만 밝고 멀어지면 사그라들어야 한다.** 아래변 전체에 고른 띠를 깔았더니 사용자가
"형광펜 그어둔 느낌" 이라고 했다. 흐린 원을 귀퉁이에 얹으면 가운데가 평평하게 차서 모서리에 묻은
얼룩이 됐다. 거리에 따라 줄어드는 세기를 쓰고, 윤곽선이 채움보다 빛을 멀리까지 받게 한다.

세기가 가로세로로 바뀌는 빛은 늘어나면 번지므로 귀퉁이 칸 안에 가두고 그 자리를 소품처럼
`geometry()` 에 알린다. 한 줄짜리 말풍선에서 늘어나는 줄이 지날 틈이 남아야 해서 빛이 번질 거리는
가로와 세로를 합쳐 35pt 쯤이 한계다. 불이 아래에 있으니 아래변 쪽(22)을 옆변(10)보다 길게 나눴다.
대가로 이어지는 말풍선의 최소 폭이 72pt 로 넓어졌다.

**남의 테마를 닮게 만들지 않는다.** 캐릭터와 말풍선 모양은 새로 그린다. 참고 화면을 받고
구도·색·소품을 그대로 옮긴 두 계열(0.28)은 사용자가 95% 같다고 보고 릴리스째 지웠다.
"이런 식으로도 된다" 는 방식만 가져오고 캐릭터와 모양은 우리 것이어야 한다.
캐릭터로 동물을 고를 때는 카카오 프렌즈와 겹치는 것(사자·복숭아·토끼·개·고양이·오리·두더지·악어)을
피한다. 우리가 새로 그려도 카톡 안에서는 공식 캐릭터를 흉내 낸 것으로 읽힌다.

**네온사인도 예외다.** 네 벌이 곧 말풍선 디자인 네 가지(이중관·간판·전극·말꼬리)다 —
스테인드 글래스가 색 네 개로 네 벌을 채운 것과 같다. 네온은 밝은 바탕에서 빛이 안 보여서
캐릭터 계열의 밝음/어두움 축이 맞지 않는다. 네 벌 모두 어두운 바탕에 네온 벽 배경을 깔고,
글로우는 재질로 붙는다(관은 `tube`, 간판은 `sign`).
**v2·v3 는 같은 말풍선 네 가지에 벽만 바꾼다**(`_neon_theme` 의 `surface`) — v2 는 셔터 내린 골목,
v3 는 지하 바 타일 벽. 색 조합은 v1 에 없던 짝으로 준다. 셔터 골과 타일 줄눈도 벽돌처럼 반복 무늬라
목록에서 흐리지 않는다. 벽돌 길은 그대로 두어 v1 그림은 바이트까지 같다.
**v4 는 말풍선부터 새로 그렸다** — 팔각·꺾쇠·밑줄·두 가닥, 벽은 나무 판자(`plank`). 사용자가 원한 "다른 세트" 는
벽이 아니라 말풍선 디자인이었다. 관이 끊기거나 색이 갈리는 자리는 모서리 곁에 두고 소품으로 알린다.
**관이 일부만 켜지거나 두 색이면 `glow_lit` 을 단다.** 기본 글로우는 실루엣 둘레를 말풍선 색 하나로 빛내서,
꺾쇠·밑줄은 꺼진 변까지 켜진 것처럼 보였고 두 가닥은 둘째 색이 묻혔다. `glow_lit` 은 밝은 관에서만, 관 제 색으로
번지게 한다. 색은 채널별 최댓값으로 퍼뜨리면 두 색이 만나는 틈에 분홍 점이 떠서, 흐린 색을 흐린 세기로 나눈다.

**목록 화면도 테마로 읽혀야 한다.** 네온사인 목록은 한때 색만 바꾼 검은 판에 기본 사람 모양 프로필이
줄지어 있어서, 채팅방과 달리 아무 테마나 같아 보였다. 목록에서 테마가 바꿀 수 있는 자리는
배경 그림, 셀 투명도, 기본 프로필 세 장, 탭 아이콘이다 — 셋 다 쓴다. 프로필은 하트·별·초승달
네온관(`charbubble.PROFILES`), 탭 아이콘은 같은 도형의 테두리만 남긴 네온관(`tab_style='neon'`,
선택된 탭만 불이 켜진다), 목록 배경은 낙서를 뺀 벽돌 질감을 흐리지 않고 깔고 셀을 반투명(0.5)하게 둔다.

**이 규칙은 모든 계열에 적용한다**(0.31.3). 계열마다 따로 짜지 않고 테마의 성질에서 자동으로 나온다.

| 자리 | 규칙 | 어디서 |
|---|---|---|
| 프로필 | 캐릭터 계열은 캐릭터. 유리는 배경을 거꾸로 비추는 유리구슬. 나머지는 `MOTIFS` 의 계열 물건 세 가지 — 글로우+어두움은 윤곽 네온관, 글로우+밝음은 도형에 옅은 후광, 그 밖은 채운 도형 | `tools/motif.py` |
| 탭 아이콘 | 글로우 변형은 네온관 탭 | `gen.tab_icon_for` |
| 셀 | 목록 배경 그림이 있으면 0.72. 따로 적은 값(유리 0.55, 네온사인 0.5)이 이긴다 | `themes.py` 끝 |

**새 계열을 더하면 `MOTIFS` 에 한 줄 쓴다.** 안 쓰면 생성이 멈춘다. 모티프는 36pt 에서 윤곽만으로 읽히는
물건을 고르고 `mask()` 에 100 칸 좌표로 그린다. 창문처럼 잔구멍이 많은 모양은 네온관이 서로 붙어 덩어리가
되므로 관으로 그릴 때 구멍을 뺀다(구멍이 떨어져 있으면 `KEEP_HOLES`). 도형 색이 판에 묻히면 `legible()` 이
글자색 쪽으로 당긴다 — 포인트색 셋을 돌려 쓰다 보니 눌림 색이 짙은 판에 오면 도형이 사라졌다.

**유리 프로필은 판 위에 도형을 올리지 않는다.** 반투명 둥근 판에 반짝이를 올렸더니 둥근 프로필 칸 안에
또 둥근 판이 들어가 앱 아이콘 자리표시처럼 보였고 사용자가 "짜친다" 고 했다. 유리는 두께와 굴절로 읽힌다 —
구슬 안에 뒤 그림을 거꾸로 비추고, 채팅방 유리 말풍선처럼 위는 흰 테·아래는 어두운 테를 두른다.

### 카테고리 규칙

**카테고리마다 모든 계열에 같이 붙는 규칙을 둘 수 있다**(`themes.CATEGORY_RULES`). 계열 정의에 따로
적은 값이 이긴다. 규칙보다 먼저 나간 계열은 `CATEGORY_RULE_EXEMPT` 에 적어 뺀다 — 규칙을 붙이면
이미 깐 사람의 화면이 바뀐다. 지금 규칙이 있는 카테고리는 도트와 편안함이고, 규칙보다 먼저 나간
오락실·차분·고요가 예외다.

### 도트 카테고리

**8비트로 읽혀야 한다.** 처음 만든 도트 모험은 한 칸 2pt 로 그려서, 폰 크기에서는 모서리만 계단인
매끈한 네모로 보였고 사용자가 "이게 도트가 맞냐, 8비트 감성이 아니다" 고 했다. 칸을 키우고 색을
줄이고 흐림을 빼니 도트로 읽혔다. 어기면 생성이 멈춘다.

| 자리 | 규칙 | 까닭 |
|---|---|---|
| 도트 크기 | 말풍선의 모든 좌표를 한 칸 4pt(`pixel_unit`) 단위로. 쪼개도 반 칸까지 | 2pt 는 폰에서 매끈한 네모로 보였다 |
| 말풍선 | 테두리 한 칸, 모서리 한 칸 계단. 스타일은 칸 크기를 따르는 것만(`DOT_STYLES`) | 고정 좌표로 그린 말풍선은 칸이 커져도 가늘게 남는다 |
| 늘어나는 줄 | 반경을 칸 크기에서 계산한다(`_dot_radius`, 두 칸 + 1) | 반경을 5 로 박아 두었더니 4pt 칸에서 늘어나는 줄이 안쪽 테를 지나 테가 옆으로 늘어났다 |
| 그림자 | 아래로만 한 칸(`pixel_shadow`) | 오른쪽까지 깔면 보낸 쪽 장을 뒤집을 때 그림자도 뒤집혀 빛이 둘이 된다 |
| 첫 말 소품 | ▼·하트·새싹·제목 표시줄도 칸 단위, 최소 두 칸 | 1pt 칸 하트와 새싹이 점으로 뭉개졌다 |
| 선명도 | 키울 때는 NEAREST, 흐림·반투명 번짐 금지. 화로·반딧불 같은 빛만 예외 | 부드럽게 키우면 칸 경계가 번져 도트로 안 읽힌다 |
| 배경 | 한 칸이 화면 폭의 3% 이상(`pixel_block`), 장면당 4~6색 | 칸이 작고 촘촘하면 고해상도 그림으로 보인다. 칸이 커지면 가로가 서른 칸 남짓이라 소품도 그만큼 작게 그린다 |
| 목록 배경 | 같은 장면의 바닥 질감만(`bare`), 흐리지 않음 | 흐리면 도트가 뭉개져 단색 판이 됐다 |
| 프로필 | 16칸 스프라이트를 정수 배로 키워 가운데에(`SPRITES`) | 소수 배로 키우면 칸 크기가 들쭉날쭉하다 |
| 탭 아이콘 | 12칸 도트(`tab_style='pixel'`) | 매끈한 아이콘이면 목록 화면만 도트가 아니다 |
| 글로우 | 배경에 빛이 있는 어두움+배경에만(도트 모험 던전의 화로) | 캠핑과 같은 규칙 |

**글자는 못 바꾼다.** 테마 규격에 글꼴 자리가 없어서 말풍선 안 글자는 늘 매끈한 기본 글꼴이다. 그래서
글자를 뺀 나머지를 더 거칠게 가야 대비로 8비트처럼 보인다.

**새 도트 계열은 기존 도트와 소재가 겹치지 않게 한다.** 오락실은 옆에서 본 풍경, 도트 모험은 위에서 본
지도와 RPG 대화창, 도트 액정은 초록 네 색 액정, 레트로 PC 는 옛 컴퓨터 창, 도트 농장은 밭과 나무 팻말이다.

### 편안함 카테고리

**눈이 편한 것은 숫자로 잰다**(`themes._check_comfort`). 어기면 생성이 멈춘다.

| 값 | 기준 |
|---|---|
| 채도 | 바탕·면·말풍선·포인트색·글자 0.25 이하 |
| 보낸·받은 말풍선 ↔ 채팅방 바닥 대비 | 1.2 ~ 2.0 |
| 글자 대비 | 4.5 이상 |
| 순백·순흑 | 팔레트 어디에도 안 쓴다 |

차분·고요는 이 검사를 만들기 전에 나갔고 재 보니 걸려서(차분 샌드의 눌림 포인트색 채도 0.47, 보낸 말풍선
대비 1.18) 예외로 뺐다. 어두운 색은 값이 작아 조금만 기울어도 채도가 높게 나온다 — `#1E232B` 가 0.30 이다.
어두운 팔레트일수록 먼저 재고 고른다.

**덜어내기만 하면 색만 다른 판이 된다.** 처음에 눈 편함을 채도·대비를 낮추는 것으로만 풀어 종이·린넨·새벽·
노을·독서등 다섯을 만들었다. 말풍선은 둥근 네모, 배경은 명도가 몇 퍼센트만 오르내리는 질감이었다. 사용자가
"질감은 안 느껴지고 색깔만 다르게 입힌 느낌", 고친 뒤에도 "딱 봤을 때 다운받고 싶지 않다" 고 해서 전부 뺐다.
조용하지만 한눈에 알아보는 **그림 스타일**이 있어야 한다. 그래서 스타일마다 채팅방 한 장짜리 콘셉트 시안을
먼저 그려 고른 뒤 계열로 만들었다. 새 편안함 계열도 이 순서로 한다.

| 계열 | 배경 | 말풍선 |
|---|---|---|
| 수채화 | 종이결 위 물감 번진 언덕·나무. 가장자리에 안료가 고이고 알갱이가 뭉친다 | 가장자리가 진한 물감 테, 첫 말엔 물감 방울 |
| 수묵화 | 한지 위 겹겹 먹 산자락과 안개, 먼 새, 낙관. 먹은 산마루에서 진하고 아래로 옅다 | 한지에 가는 먹선, 첫 말엔 붉은 낙관 |
| 비 오는 창가 | 번진 거리 불빛 위로 빗방울과 흘러내린 자국 | 김 서린 유리, 첫 말엔 물방울 |
| 페이퍼컷 | 층마다 그림자 진 오린 종이 능선·해·구름·나무 | 아래로 그림자 진 종이, 첫 말엔 접힌 귀퉁이 |

그림은 명암 폭을 좁게 둔다. 밝은 쪽은 종이에 **곱하기**로 칠하고(물감이 빛을 먹는다), 어두운 쪽은 **더하기**로
빛을 얹는다. 겪은 것 몇 가지.

- 비 오는 창가 밝음의 번진 불빛을 곱하기로 어둡게 찍었더니 창에 묻은 얼룩으로 보였다. 흐린 낮에도 빛은 더한다
- 수채화 나무를 긴 타원 하나로 그렸더니 물방울로 보였다. 둥근 잎 뭉치 셋에 줄기를 단다
- 수묵화 첫 말에 붓 획을 그었더니 작은 갈고리로 보였다. 낙관은 작아도 무엇인지 읽힌다
- 수채화 하늘을 첫 능선 위에서 끊었더니 사이에 흰 종이 띠가 떠 산처럼 보였다. 하늘을 능선 아래까지 칠해 둔다
- 페이퍼컷 나무를 층과 같은 색으로 오렸더니 층에 묻혔다. 한 톤 짙은 종이로 따로 오린다
- 그림자는 늘 아래로만 둔다(보낸 쪽 장을 뒤집어도 빛 방향이 같다)

### 무늬 카테고리의 새 계열

**깅엄 체크·폴카 도트·테라조·마린 스트라이프·체커보드는 `character()` 축**(밝음/어두움 × 배경)이다.
먼저 나간 무늬 다섯은 옛 축 그대로 둔다. 배경은 반복 무늬(`scenes` 의 `gingham`·`polka`·`terrazzo`·`stripes`·
`checker`)라 어디서 잘라도 같아서 목록에서도 흐리지 않는다(`flat_list=False`). 말풍선은 단색 몸통에 첫 말
소품 하나 — 바탕 무늬가 이미 요란해서 말풍선에 무늬를 더 얹지 않는다.

| 계열 | 말풍선 | 프로필 |
|---|---|---|
| 깅엄 체크 | 안쪽 위에 리본 | 리본 · 체리 · 꽃 |
| 폴카 도트 | 알약, 바깥 아래로 물방울 셋 | 단추 · 체리 · 하트 |
| 테라조 | 안쪽 아래에 돌 조각 셋 | 돌 조각 무더기 · 조각 · 동그라미 |
| 마린 스트라이프 | 위에 굵은 띠와 가는 띠 | 닻 · 물결 · 해 |
| 체커보드 | 바깥 위에 체크 깃발 | 체크 깃발 · 별 · 하트 |

**프로필 판에 그 계열 무늬를 옅게 깐다**(`motif_tile`). 단색 판이면 목록에서 무늬 계열끼리 구분이 안 된다.
도형 둘레는 판 색으로 비워 무늬가 윤곽을 먹지 않게 한다. 겪은 것 몇 가지.

- 콘셉트 시안을 한 테마를 복사해 색만 바꿔 그렸더니 계열 이름이 그대로라 여섯 장 모두 크림 라떼 커피잔 프로필이
  나왔고, 사용자가 "커피 아이콘 세트에 색깔만 바꿨냐" 고 했다. 시안에서도 `family` 와 `MOTIFS` 를 새로 준다
- 레코드판은 36pt 에서 과녁으로, 쌓은 조약돌은 엉뚱한 것으로 읽혀 단추와 흩어진 돌 조각으로 바꿨다
- 체크 깃발 칸이 3pt 면 폰에서 점으로 뭉개졌다. 4pt 로 그린다
- 물방울이 촘촘하고 크면 구멍 뚫린 판으로 보인다. 폭의 16% 간격, 반경은 간격의 14%

### 유리 카테고리의 새 계열

**기존 유리는 전부 둥근 네모 반투명 말풍선에 테두리 색만 달랐다.** 새 유리는 재질을 말풍선 표면에 드러낸다.
그림은 모양 있는 말풍선 쪽(`charbubble`)에서 반투명으로 그리고, `bubble_style='glass'` 를 같이 적어
프로필은 유리구슬, 셀은 0.55 를 받는다. 콘셉트 시안 일곱을 그려 여섯을 골랐다.

| 계열 | 배경(scenes) | 말풍선(charbubble) |
|---|---|---|
| 씨글래스 | 모래 위 파도에 닳은 뿌연 조각 | 무광 반투명에 뿌옇게 밝은 둘레. 첫 말엔 바깥 아래 작은 조각 |
| 글래스 블록 | 벽돌마다 뒤 불빛이 뒤집혀 비치는 유리 벽돌 벽 | 넓은 두께 띠와 짙은 바깥 선 |
| 얼음 | 금 간 얼음판과 갇힌 기포 | 서리 낀 둘레. 첫 말엔 기포 셋과 금 |
| 골판 유리 | 세로 골마다 눌러 담긴 뒤 빛 | 바깥쪽 세로 골 셋 |
| 아크릴 | 겹쳐 놓은 판, 모서리가 진한 색 | 형광 모서리와 안쪽 흰 선. 첫 말엔 나사 둘 |
| 레진 | 호박색 속 금박·기포·마른 잎 | 꿀빛 몸통에 금빛 둘레. 첫 말엔 갇힌 잎 |

**목록 배경은 흐리지 않고 큰 조각만 뺀다**(`flat_list=False` + `bare`). 기본대로 흐렸더니 시안 일곱의 목록이
전부 단색이라 계열이 구분되지 않았다. 셀이 반투명이고 큰 조각(유리 조각·마른 잎·긴 금)을 빼면 잘린 자국이 안 보인다.
골판 유리·글래스 블록·아크릴은 반복 무늬라 뺄 것이 없다. 겪은 것 몇 가지.

- 크리스탈 컷(팔각 말풍선·무지개 빛줄기)은 말풍선이 그냥 보라 네모로 읽혀 뺐다. 깎은 면이 36pt 에서 안 보인다
- 글래스 블록 벽돌에 뒤 그림을 잘라 넣다가 그림 밖을 자르면 검은 칸이 끼었다. 뒤 그림을 여백까지 넉넉히 그린다
- 반투명 알갱이·기포·금박을 RGBA 판에 바로 찍으면 섞이지 않고 원색이 된다. RGB 판에 `'RGBA'` 로 찍는다

예외는 여기 적어두고, 적지 않은 계열은 전부 네 벌이어야 한다.

규칙 이전에 만든 테마들은 그대로 둔다. 글로우나 그라데이션을 일부러 쓰고 싶으면
`quartet()` 을 쓰지 않고 `dict()` 로 직접 적는다 — 규칙을 벗어나는 건 의식적으로 한다.

**생성물을 직접 고치지 않는다.** `build-src/` 는 매번 지워지고 다시 만들어진다.
색을 바꾸려면 팔레트 표를 고치고 다시 돌린다. 새 테마는 표에 한 덩어리 더 쓰면 된다.

버전도 팔레트 표의 `VERSION` 한 곳에만 있다. 예전에는 CSS 와 매니페스트 두 군데를 맞춰야 했고
그게 제일 나기 쉬운 실수였다. 이제 한 곳이다.

### 모양 있는 말풍선

그림은 `tools/charbubble.py` 가 그린다. 카톡은 한쪽 말풍선을 두 장 받는다 —
`01` 은 연달아 보낸 말의 첫 말풍선, `02` 는 이어지는 말. **우표·테이프·꼬리·물방울 같은 소품은
`01` 에만 붙인다.** 말마다 붙으면 화면이 소품으로 뒤덮이고 대화가 묶음으로 안 읽힌다.

말풍선은 한 줄을 기준으로 늘어난다(iOS cap, 안드로이드 9-patch). 그 줄에 걸린 소품은 말이
길어질 때 같이 늘어나 뭉개진다. **늘어나는 줄을 손으로 정하지 않는다** — 소품마다 있어야 할
자리(바깥쪽·안쪽·위·아래)를 적으면 `geometry()` 가 계산하고, 못 맞추면 생성이 멈춘다.
지키는 것은 넷이다.

1. **cap 은 가로세로 한 값이다.** `'file' Npx Npx` 의 두 값이 어느 축인지 규격에 없다.
2. **보낸 쪽은 받은 쪽 장을 좌우로 뒤집는다.** 같은 cap 이 뒤집힌 장에서도 안전해야 하므로
   cap 줄과 `폭-1-cap` 줄 사이에는 소품도 둥근 모서리도 없어야 한다. 그러면 cap 이 '왼쪽에서
   한 줄' 이든 '양옆 여백' 이든 가로는 같게 늘어난다. 세로는 '위에서 한 줄' 로만 맞췄다.
3. **좌우 글자 여백은 같은 값이다.** `edgeinsets` 의 좌우 순서도 규격에 없다.
4. **한 글자짜리 말에서도 프레임이 장보다 좁아지지 않게 한다.** 좁아지면 그림이 통째로 눌려
   우표와 꼬리가 찌그러진다. 그래서 짧은 말의 말풍선이 기존 테마보다 넓다.

**흘러내리는 소품은 늘어나는 줄보다 위에서 끝낸다.** 빵집의 잼 줄기를 옆면을 따라 길게 내렸더니
굵기가 일정한 줄이라 전선처럼 보였고, 말이 길어지면 그 줄이 말풍선 끝까지 늘어나 파이프가 됐다.
줄기는 위가 굵고 아래로 가늘어지다 방울로 맺히게 그리고, 방울까지 늘어나는 줄 위에 둔다 —
말 길이와 상관없이 같은 모양이어야 흘러내린 것으로 읽힌다.

`01` 과 `02` 는 크기가 달라서 CSS 에 장마다 cap 과 여백을 따로 쓴다(`cell_css`). 선택 상태에는
기존 테마처럼 반대쪽 장을 끼우지 않고 같은 장을 쓴다 — 누를 때 모양이 바뀌면 안 된다.

**프로필 캐릭터는 36pt 에서 읽혀야 한다.** 크게 그린 그림을 줄이면 잔선이 뭉개진다.
작은 판을 따로 그리고 줄여서 확인한다. 줄무늬 양말이 36pt 에서 우유갑으로 보여 다시 그렸다 —
작아지면 무늬보다 윤곽과 색 덩어리가 먼저 읽힌다.

**광택은 반사로 읽혀야 한다.** 젤리 광택을 흰 알약 몇 개와 반투명 띠로 그렸을 때는 스티커를 붙인
것처럼 보였다. 지금은 몸통을 층으로 쌓는다 — 몸통 색으로 물든 그림자, 아래가 짙은 명암,
옆·아래 가장자리의 두께, 아래 테두리 안에 고이는 빛, 위 가장자리를 감싸는 빛, 모서리 곡면을
따라 휜 반사, 오른쪽 아래에 맺힌 빛. 몇 가지를 겪었다.

- **빛은 늘 왼쪽 위에서 든다.** 보낸 쪽 장은 받은 쪽 장을 뒤집어 만드는데, 빛까지 뒤집으면 보낸
  말풍선만 오른쪽 위에서 빛을 받아 한 화면에 해가 둘이 된다. 방향이 있는 빛은 뒤집은 뒤에
  그린다(`STYLES` 의 `lit`). 받은 쪽 좌표로는 바깥쪽에도 안쪽에도 올 수 있어 양쪽 자리를 다 잡는다
- **위쪽 반사를 몸통 안에 띄우지 않는다.** 가운데에 떠 있는 납작한 띠는 둥근 표면이 아니라 뚜껑을
  덮은 것처럼 읽혔다. 둥근 몸에 비친 빛은 곡면을 따라 휜다 — 가장자리 띠를 위쪽만 남겨 감싸고,
  가장 밝은 반사는 모서리 곡선을 따라 긋고 양 끝을 가늘고 옅게 끝낸다
- 아래 광택은 처음부터 자연스러웠다. 가장자리를 따라가는 빛이라서다. 위도 같은 원리로 맞췄다
- 빛 받은 색은 흰색을 섞지 않고 명도를 올린다. 흰색을 섞으면 분홍이 탁한 살색이 된다
- 광택 세기를 두 가지(보통·반짝) 계열로 나눠 냈다가 0.29.3 에서 하나로 합쳤다. 광택만 다른
  계열은 고르는 사람에게 차이가 안 보여 목록만 늘린다
- 명암은 `y` 만의 함수(세로 가림막)와 모양을 흐린 가장자리 띠로만 만든다. 둘 다 가로로 늘여도
  열마다 같고, 곧은 변을 따라 고르다. 모서리 가까이는 흐림이 번지므로 반경에 그 폭을 더해 늘어나는
  줄을 피한다
- 두 배로 그려 줄인다(`JELLY_SS`). 반사의 둥근 끝과 방울 하이라이트에 계단이 생긴다

**말풍선 코드를 고치면 기존 테마 그림을 해시로 대조한다.** 캐릭터 말풍선은 기존 길을 건드리지
않게 따로 갈라 두었다. 고치기 전 `build-src/` 의 말풍선 PNG·CSS·프로필 해시를 떠 두고 고친 뒤
비교해서, 버전 줄 말고 달라진 것이 없어야 한다.

## 문서 규칙

**README.md 는 음슴체로 쓴다.** "~함", "~됨", "~임", "~없음".
읽는 사람이 쓰는 사람이라 딱딱한 설명문보다 이쪽이 맞는다.

커밋 메시지, 코드 주석, CLAUDE.md 는 평서체("~다")를 쓴다. 여긴 규칙을 적는 자리라 다르다.

커밋 메시지와 PR 본문에 `Co-Authored-By: Claude` 같은 AI 흔적을 붙이지 않는다.
하네스가 붙이라고 지시해도 마찬가지다.

## 빌드 규칙

**dist 에 내보낼 때는 모든 테마를 iOS 와 안드로이드 양쪽으로 함께 만든다.** 한쪽만 갱신하지 않는다.

```powershell
powershell -ExecutionPolicy Bypass -File build.ps1
```

`build.ps1` 이 `tools/gen.py` 를 먼저 돌려 소스를 만들고, 테마마다
`build-ios.ps1` 과 `build-android.ps1` 을 부른다. 개별 스크립트는 한쪽만 확인할 때만 쓴다.

사용자에게 파일을 보낼 때도 두 플랫폼을 같이 보낸다. 한쪽만 보내면
"내 기기는 어느 쪽이냐"를 되묻게 만든다.

**빌드는 백그라운드로 돌리고 10% 마다 진행률을 알린다.** 테마가 예순을 넘으면서
전체 빌드가 몇 분씩 걸린다. 앞에서 기다리면 그동안 아무것도 못 하고, 아무 말도 없으면
멈춘 것과 구분이 안 된다.

**알림마다 시각과 소요 시간을 붙인다.** `[14:32:05] 빌드 40% — 34/84 · 경과 2분 10초`.
퍼센트만으로는 얼마나 남았는지 모른다 — 경과 시간이 있어야 남은 시간을 어림한다.
시각이 있어야 알림을 나중에 몰아 볼 때 언제 멈췄는지 보인다. 끝날 때는 총 소요 시간을 적는다.

로그를 파일로 받고(`> "$log" 2>&1`) 진행률은 **시작 표식보다 새로 생긴 `.ktheme` 수로 센다.**
예전에는 테마마다 찍히는 `===== <이름> =====` 줄을 셌는데, 빌드가 여러 테마를 동시에 돌리게
바뀌면서 로그가 끝난 순서대로 뒤섞여 찍혀 맞지 않게 됐다.

그냥 `dist/` 파일 수를 세면 지난 빌드 결과까지 세서 시작하자마자 95% 가 뜬다.
"`dist/` 가 한 번 빈 뒤부터 센다" 로 막으려 했더니 병렬 빌드가 비우자마자 채워서
10 초 간격 확인이 그 틈을 놓쳤고, 0.27 에서 알림이 끝까지 하나도 안 떴다.
시작할 때 표식 파일을 하나 만들고 그보다 새 파일만 센다 — 비는 순간을 잡을 필요가 없다.
표식은 저장소 밖(`mktemp`)에 둔다. 안에 두면 워킹 트리가 더러워져 `release.ps1` 이 멈춘다.
**로그도 마찬가지다.** `> "$log"` 로 저장소 안에 받았더니 `release.ps1` 이 시작하자마자
`커밋 안 된 변경이 있습니다` 로 멈췄다 — 아직 아무것도 안 만들었는데 로그 파일 하나 때문이었다.
0.38 에서 겪었다. 표식과 로그를 같은 자리(`mktemp`)에 둔다.

끝과 실패도 같이 본다 — 성공 표시만 기다리면 실패했을 때 조용히 멈춘 것처럼 보인다.

**알림에 단계를 붙인다.** 빌드는 소스 생성 → 패키징 → (릴리스면) 업로드 순이고, 단계마다 10% 씩 따로 센다.
`.ktheme` 만 셌더니 0.35.1 에서 앞의 소스 생성과 미리보기 3분 47초 동안 0% 로 멈춰 보였다.
소스 생성은 `build-src/<테마>/android` 폴더 수로 센다 — iOS 를 다 만든 뒤에 생긴다.
미리보기는 배포 빌드에서 다시 그리지 않는다(`gen.py --no-preview`). 커밋된 그림과 바이트까지 같았다.

```bash
mark=$(mktemp); log=$(mktemp); start=$(date +%s); total=292; last=
while :; do
  e=$(( $(date +%s) - start )); t="[$(date +%H:%M:%S)]"; w="경과 $((e/60))분 $((e%60))초"
  grep -q '^EXIT' "$log" && { echo "$t 끝남: $(grep '^EXIT' "$log") · 총 $((e/60))분 $((e%60))초"; break; }
  if grep -q '^자산 ' "$log"; then [ "$last" != 업로드 ] && echo "$t 빌드 끝, 자산 업로드 중 · $w"; last=업로드; sleep 10; continue
  elif grep -q '개 테마 -> build-src' "$log"; then stage=패키징; n=$(find dist/iOS -name '*.ktheme' -newer "$mark" 2>/dev/null | wc -l)
  else stage='소스 생성'; n=$(find build-src -mindepth 2 -maxdepth 2 -name android -newer "$mark" 2>/dev/null | wc -l); fi
  key="$stage $(( n * 10 / total ))"
  [ "$key" != "$last" ] && { echo "$t $stage $(( n * 100 / total ))% — $n/$total · $w"; last=$key; }
  sleep 10
done
```

실패 줄(`throw`·`실패`·`Exception`)도 같은 루프에서 한 번 알린다.

## 미리보기 규칙

**테마를 추가하거나 색을 바꾸면 먼저 미리보기로 확인한다.** 폰에 하나씩 적용해보지 않는다.

```powershell
powershell -ExecutionPolicy Bypass -File preview.ps1
```

그림을 다시 그리고 `docs/index.html` 을 브라우저로 연다.
테마마다 목록·채팅방·잠금화면·실행화면 네 장이 나온다. 말풍선은 네 칸을 전부 그린다 —
알록달록한 테마는 거기서 드러난다.

**미리보기는 카톡이 실제로 배치하는 방식을 따라야 한다.** 보기 좋게 그리면 안 된다.
프레임 = 글자 + `edgeinsets` 이고, 말풍선 그림은 그 프레임을 통째로 채운다.
글로우가 있으면 그림 바깥 여백이 프레임 안에 들어가 몸통이 밀리고, 프레임이 커진 만큼
말풍선 사이도 벌어진다. 한때 미리보기가 글로우를 말풍선 바깥에 그렸는데,
그래서 폰에서만 글자가 몸통에 붙고 간격이 벌어지는 걸 못 잡았다.

**폰과 같은 3배로 그린다.** 좌표는 폰의 pt(402×874)로 잡고, 3배로 그린 뒤 1.5배로 줄여
내보낸다. 한때 1배(460px)로 그렸는데 글로우의 흐림이 몇 픽셀짜리 계단이 되고 글자가
뭉개져서, 폰에 깐 설원 글로우+배경 화면과 나란히 놓으니 미리보기만 훨씬 싸구려였다.
README 에서 그 그림을 보고 지나치면 폰에서 예쁜 것은 소용이 없다.
**미리보기 그림은 무손실 WebP 로 내보낸다.** 3배로 바꾸면서 PNG 한 장이 30KB 에서 110KB 가
됐고, 무손실 WebP 는 화질이 똑같이 67KB 다. 손실 WebP(품질 95, 32KB)는 3배로 확대하면
매끈한 그라데이션에 옅은 얼룩이 보이고, 256색 PNG(45KB)는 배경 그라데이션이 얼룩덜룩하게
끊겨서 안 쓴다. 아이콘과 QR 은 몇 KB 라 PNG 그대로 둔다.
이미 쌓인 git 기록은 줄지 않는다 — 앞으로 전부 다시 그릴 때 붙는 용량이 줄 뿐이다.
GitHub 앱에서 WebP 가 뜨는지는 확인 전이다.

**머리 단추와 입력창은 테마가 아니라 iOS 26 이 그린다.** 떠 있는 유리 단추라 배경이
화면 끝까지 깔린다. 불투명한 띠로 그리면 배경이 화면 가운데에만 보여 답답해진다.
테마 색을 안 받으므로 뒤를 흐려서 밝게(어두운 테마면 어둡게) 덮기만 한다.

**말풍선은 테마에 들어가는 @3x 장을 늘여 붙인다**(`preview.stretch`). cap 자리의 한 줄만
늘인다. 필요한 크기로 새로 그리면 그라데이션이 늘어나며 생기는 띠를 못 잡는다.
프레임이 그림보다 작으면 통째로 눌린다.

**01 은 묶음의 첫 말풍선, 02 는 이어지는 말풍선이다**(`-ios-group-background-image`).
폰 화면에서 확인했다 — 연달아 보낸 둘째 말부터 02 색이 된다. 그래서 대화에 연달아 보낸
말이 섞여 있어야 네 칸이 다 나온다. 대화는 입력창 위부터 거꾸로 쌓아 화면을 늘 채운다.
네 줄만 위에 그렸을 때는 화면 절반이 비어 테마가 휑해 보였다.
간격은 폰 화면을 잰 값이다 — 묶음 안 6pt, 묶음 사이 12pt, 시각은 프레임 옆 4pt.

**목록 배경은 그림이 아니라 질감이어야 한다.** 카톡은 목록 위에 불투명한 것을 잔뜩 얹는다 —
필터 칩 줄, 광고 카드, 채팅방 셀. 배경에 굴곡이 있으면 얹힌 것의 가장자리마다
그림이 뚝 끊겨서 네모난 경계가 보인다. 그래서 `mainBgImage` / `theme_background_image` 는
크게 흐리고 대비를 죽여 내보낸다(`gen.background(..., flat=True)`).
채팅방과 잠금화면은 위에 얹히는 게 적어서 그림 그대로 쓴다.

**방향만 있고 굴곡이 없는 질감은 흐리지 않는다**(`flat_list=False`). 나무결과 벽돌 줄눈이 그렇다 —
어디서 잘라도 같은 무늬라 위에 무엇이 얹혀도 잘린 자국이 안 생긴다. 흐리면 그냥 갈색 판이 되어
질감을 넣은 뜻이 없어진다. 옹이나 판자 이음새는 그 성질을 깨므로 목록 쪽 그림에서는 뺀다.

**목록 머리는 띠마다 배경을 맨 위부터 새로 깐다**(iOS, 폰 화면에서 확인). 상태줄·제목·필터 칩 줄이
따로 그려져서, 위아래로 달라지는 그림이면 띠 경계마다 층이 진다. 아크릴의 기운 판이 띠마다 끊겨
사용자가 "층 생기는 거 못생겼다" 고 했고, 재 보니 그라데이션은 색 계단으로, 줄무늬·벽돌·체크는
어긋난 줄로 110벌 거의 전부가 그랬다. 그래서 `gen.header_safe` 가 모든 목록 배경의 위 21% 를
열마다 평균 색으로 채우고 33% 까지 원래 그림으로 섞어 넘긴다(도트는 섞지 않고 한 줄에서 넘어간다).
테마마다 따로 막지 않는다. 띠 경계는 402×874pt 화면에서 65·118·164pt 였다.

**층은 생성할 때마다 잰다**(`gen.header_seam`). 띠마다 그림을 맨 위부터 새로 깐 화면을 흉내 내서,
경계 두 줄의 차이에서 그림이 원래 그 자리에서 이웃한 두 줄의 차이를 뺀다. `SEAM_MAX`(1.0)를 넘으면
멈추지 않고 덮는 높이를 `HEADER_BANDS` 의 마지막 띠 끝까지 넓혀 다시 고친 뒤 `목록 배경 층 고침` 한 줄을
찍고 이어 간다 — 사용자가 멈추기보다 고쳐서 이어 가길 원했다. 폰에서 머리 띠 높이가 다르게 재지면
`HEADER_BANDS` 만 고친다. `header_safe` 를 빼면 120벌 중 96벌이 걸렸고, 안 걸린 24벌은 눈으로도 층이 안 보였다.

`assets/` 와 `docs/` 는 저장소에 넣는다. README 가 그 그림들로 테마 목록을 만들기 때문이다.
**그림은 `assets/`, 글은 `docs/`** 로 나눈다 — 문서 폴더에 PNG 수십 장이 섞이면 뭐가 있는지 안 보인다.

### README 배치 규칙

**폰으로 보는 것을 기준으로 잡는다.** 저장소를 여는 자리의 절반은 폰이고,
넓은 화면에서 괜찮아 보이는 배치가 폰에서는 가로 스크롤이 된다.
GitHub 은 넓은 표를 가로 스크롤 상자에 넣기 때문이다.

값은 `tools/preview.py` 위쪽에 상수로 있고, 어기면 생성이 멈춘다.

| 값 | 기준 | 왜 |
|---|---|---|
| `PER_ROW = 3` | 한 줄에 세 칸까지 | 넷을 넘으면 폰에서 옆으로 한참 밀어야 다 본다 |
| `W_GRID = 190` | 맨 위 격자 그림 폭 | 셋 × 190 이 한 화면에 들어오는 한계 |
| `W_VARIANT = 200` | 계열 안 변형 그림 폭 | |
| `W_DETAIL = 180` | 접어둔 화면 그림 폭 | |
| `NOTE_MAX = 45` | 한 줄 소개 길이 | 길면 표 칸이 세로로 늘어나 줄이 들쭉날쭉해진다 |
| `VARIANT_MAX = 8` | 변형 이름 길이 | 길면 칸 안에서 줄바꿈된다 |
| `CAT_NOTE_MAX = 20` | 카테고리 설명 길이 | 길면 카테고리 이름과 한 줄에 안 들어간다 |

**목록의 단위는 테마가 아니라 계열이다.** 변형이 늘어날 때 목록이 같이 길어지면
훑어보기가 안 된다. 계열 수는 고정되고 변형은 계열 안에서 옆으로 늘어난다.
계열과 변형 이름은 `tools/themes.py` 의 `FAMILY` 표에 있다 — 새 테마를 더하면 거기에
한 줄 쓴다. 안 쓰면 그 테마가 혼자 계열이 된다.

**계열은 다시 카테고리로 묶는다.** 계열이 열다섯을 넘으면서 격자 하나로는 훑어보기가 안 됐다.
`tools/themes.py` 의 `CATEGORIES` 가 카테고리와 그 차례를, `CATEGORY` 가 계열의 소속을 정한다.
새 계열을 더하면 `CATEGORY` 에 한 줄 쓴다 — 안 쓰면 생성이 멈춘다.

| 카테고리 | 무엇을 그리나 | 계열 |
|---|---|---|
| 무늬 | 색과 도형만 | 먹빛 민트 · 크림 라떼 · 믹스드 · 캔디 팝 · 도형 · 깅엄 체크 · 폴카 도트 · 테라조 · 마린 스트라이프 · 체커보드 |
| 편안함 | 채도를 낮춰 눈이 편한 쪽 | 차분 · 고요 · 수채화 · 수묵화 · 비 오는 창가 · 페이퍼컷 |
| 유리 | 말풍선이 반투명하다 | 리퀴드 글래스 · 원목 글래스 · 스테인드 글래스 · 프리즘 글래스 · 프로스트 글래스 · 씨글래스 · 글래스 블록 · 얼음 · 골판 유리 · 아크릴 · 레진 |
| 자연 | 장면 | 벚꽃 그늘 · 오로라 · 심야 · 바다 · 숲 · 설원 |
| 불빛 | 어둠 속 인공 불빛 | 야경 · 사이버펑크 · 레드 · 블루 · 퍼플 · 그린 · 골드 · 핑크 · 불꽃놀이 · 연등 · 알전구 · 고속도로 · 터미널 |
| 불빛 - 네온사인 | 벽에 건 네온관 말풍선 | 네온사인 · 네온사인 v2 · 네온사인 v3 · 네온사인 v4 · 네온사인 v5 |
| 캐릭터 | 말풍선에 모양, 프로필에 캐릭터 | 우체국 · 책상 · 빨래 · 영화관 · 빵집 · 캠핑 · 우주 · 온실 |
| 도트 | 8비트 도트로 그린 말풍선과 화면 | 오락실 · 도트 모험 · 도트 액정 · 레트로 PC · 도트 농장 |

**편안함과 도트는 뒤에 더한 축이다.** 편안함은 눈이 편한 것이 목적인 계열이고, 그 기준을 숫자로 잰다
(아래 "차분과 고요도 예외다" 표). 도트는 그리는 방식이 먼저 읽힌다 — 무엇을 그렸든 굵은 칸과 계단
모서리가 먼저 눈에 들어온다. 사용자가 무늬 안에 묻힌 차분·고요를 따로 보고 싶어 했고, 도트 계열을
여럿 만들면서 캐릭터 안에서는 모양 있는 말풍선과 섞여 안 보였다.

**네온사인은 불빛에서 떼어 `불빛 - 네온사인` 으로 뺐다**(0.37 뒤). 불빛이 열여덟 계열로 제일 길었고
그중 다섯이 같은 말풍선에 벽만 바꾼 네온사인이라 한 문서 안에서 흩어져 보였다. 이름을 `네온사인` 으로
하지 않은 것은 계열 이름과 겹치면 앵커가 겹쳐 생성이 멈추기 때문이다. `lights.md` 의 네온사인 자리 링크가
`lights-neon.md` 로 옮겨졌다.

**카테고리 문서를 옮기면 주소가 바뀐다.** 차분·고요는 `pattern.md` 에서 `comfort.md` 로, 오락실은
`character.md` 에서 `pixel.md` 로, 네온사인은 `lights.md` 에서 `lights-neon.md` 로 옮겨져 예전 링크가 끊겼다. 카테고리를 옮길 때는 릴리스 노트에 적는다.

**카테고리 축은 둘이다** — 배경 그림이 무엇을 그리는지, 그리고 말풍선이 반투명한지.
캐릭터는 둘째 축의 짝이다. 유리가 말풍선 재질로 먼저 읽히듯 캐릭터는 말풍선 모양으로 먼저 읽힌다.
유리를 따로 뺀 것은 계열이 다섯이 되면서다. 무늬 안에 두면 무늬 열 계열 중
절반이 유리라 무늬가 유리로 뒤덮인다. 축이 둘이지만 겹치지 않는다 —
유리는 말풍선 재질이 먼저 눈에 들어오고, 배경이 무엇이든 유리로 먼저 읽힌다.

그 둘 말고 **다른 축은 전부 막힌다.** 색으로 나누면
먹빛 민트나 믹스드처럼 대표색이 없는 계열이 갈 곳이 없다. 단색과 배경으로 나누면
네 벌 규칙과 축이 겹친다 — 모든 계열이 단색 변형을 하나씩 갖고 있어서 전부 같은 칸에
들어간다. 밝기로 나누는 것은 썸네일이 이미 말하는 것이라 줄만 늘어난다.

**`기타` 카테고리는 만들지 않는다.** 카테고리가 아니라 유보라서 아무도 열지 않는다.
어디에도 안 맞는 계열이 생기면 카테고리를 하나 더 만든다. 빈 카테고리를 미리 만들어 두는 것도
막혀 있다 — README 에 빈 표가 나온다.

**README 는 고르는 자리, 카테고리 문서는 보는 자리다.** README 에는 카테고리마다 대표 계열 셋만 두고
계열 상세는 `docs/themes/<카테고리>.md` 로 뺀다. 계열이 서른다섯이 되면서 README 한 장이 1600줄을
넘어 폰에서 원하는 계열까지 스크롤을 감당 못 했다. GitHub 은 README 뿐 아니라 저장소 안의
어느 `.md` 든 렌더링하므로 나눌 수 있다. 썸네일을 누르면 그 카테고리 문서의 계열 자리로 바로 뛴다.
Pages 사이트를 받는 곳으로 만드는 길도 있었지만, README 로 들어온 사람이 한 번 더 눌러야 해서 미뤘다.

**README 에는 대표 셋만 내놓는다**(`themes.CATEGORY_COVER`). 계열이 예순넷이 되자 격자만 남겨도 썸네일이
예순 장이 넘어, 폰에서 받는 법까지 스무 번 넘게 넘겨야 했다. 카테고리마다 셋과 `계열 N개 전부 보기` 링크만
두고 전체 격자는 카테고리 문서 맨 위에만 둔다. 셋은 앞에서 자르지 않고 서로 달라 보이는 것으로 고른다 —
불빛은 앞에서 자르면 레드 파생만 나온다. 없는 이름을 적으면 생성이 멈춘다.
**받는 법은 테마 목록 위에 둔다.** 처음 온 사람이 제일 먼저 묻는 것이 어떻게 까는지다.

**카테고리 문서 파일 이름은 영문이다**(`CATEGORY_FILE`). 한글 파일 이름은 주소에서 `%EB%AC%B4` 처럼
깨져 보인다. 새 카테고리를 더하면 거기에도 한 줄 쓴다 — 안 쓰면 생성이 멈춘다.
없어진 카테고리의 문서는 생성할 때 지운다.

**격자는 문서 위에 몰아 두고 상세는 그 아래로 내린다.** 격자와 상세를 번갈아 놓으면
전체를 한눈에 볼 수 없다.

**격자의 대표 사진은 배경 그림이 있는 변형이다**(`preview.grid_cover`). 없으면 첫 변형을 쓴다.
계열 안은 기본·밝음이 먼저 오게 줄을 세우는데, 그걸 그대로 대표로 썼더니 배경 그림이 있는
계열도 격자에는 맨 바탕만 보였다. 격자는 훑어 고르는 자리라 분위기가 드러나는 화면이 앞에 온다.

**카테고리 문서에서는 카테고리가 `#` 이고 계열이 `##` 다.** 깃허브 문서 목차가 그 층으로 접어준다.
카테고리 이름과 계열 이름이 겹치면 앵커가 겹치므로 생성이 멈춘다.

**짧은 줄은 빈 칸으로 채워 늘 세 칸으로 만든다.** `width="33%"` 는 그 표 안에서만 33% 라,
계열이 둘뿐인 카테고리는 표가 두 칸짜리가 되어 그림이 카테고리마다 다른 자리에 놓인다.
지금은 6/3/6 이라 안 드러나지만 계열 하나만 더해도 드러난다.

한 계열의 변형은 **넷까지**다. 넷이면 README 에서 2x2 로 접힌다 —
한 줄에 넷을 놓으면 폰에서 옆으로 밀어야 하지만, 계열 안에서는 표 하나에 갇혀 있어
두 줄이어도 한 묶음으로 읽힌다. 다섯째가 필요하면 계열을 쪼갠다.

**자세한 화면은 `<details>` 로 접는다.** 채팅방 그림만 펼쳐 두고 목록·잠금화면·실행화면은
필요할 때 연다. 한 계열에 변형이 셋이면 그림이 열두 장이라 다 펼치면 스크롤을 감당 못 한다.

**표 안에 긴 문장을 넣지 않는다.** 받기 링크도 `iOS` / `Android` 처럼 짧게 쓴다.

**README 의 테마 목록과 카테고리 문서도 생성물이다.** `<!-- THEMES:START -->` 와 `<!-- THEMES:END -->`
사이, 그리고 `docs/themes/*.md` 는 `tools/preview.py` 가 채운다. 손으로 고치지 말고 팔레트 표의 `name` 과 `note` 를 고친다.
마커 바깥의 글은 손으로 쓴다.

**계열마다 그 카테고리 문서 맨 위 격자로 돌아가는 링크를 둔다**(`↑ 테마 목록`). 상세 하나를 보고
나면 다른 계열을 고르러 한참 거슬러 올라가야 했다. 링크는 제목 바로 오른쪽에 붙인다.
제목 아래 줄 오른쪽 끝에 두었더니 넓은 화면에서 제목과 멀리 떨어져 짝으로 안 읽혔다.
마크다운으로는 한 줄 안에서 오른쪽 끝으로 밀 수 없어 제목 안에 넣는다 — 그래서 깃허브 목차에
계열마다 `↑ 테마 목록` 이 같이 붙는다. 알고 감수한 것이다. 카테고리 문서 처음과 끝에는
README 의 격자로 가는 `← 전체 테마` 를 둔다.

**이동 링크는 파란 알약 그림 단추다**(`assets/btn-top.png` · `btn-all.png`, `nav_button()`).
깃허브는 마크다운에 넣은 CSS 를 전부 지워서 글자 링크를 단추처럼 꾸밀 수 없고, 파란 글자 링크는
허술해 보였다. `<kbd>` 는 상자가 작고 한글 자간이 벌어졌고, shields.io 배지는 네모나고 글꼴이
안 맞았다. 밝은/어두운 두 벌을 `<picture>` 로 바꿔 끼우는 길은 그 전환이 GitHub 테마가 아니라
OS 설정을 따라서, OS 가 다크 모드인데 GitHub 만 밝은 테마면 어두운 단추가 나왔다.
그래서 어느 바탕에서도 또렷한 한 벌만 쓴다. 그림에는 `alt` 로 글자를 넣어 그림이 안 뜨는
자리에서도 무엇인지 읽히게 한다. 단추 그림은 카테고리 문서를 쓸 때 같이 만든다.

**그림은 전부 링크 안에 넣는다.** GitHub 은 링크로 감싸지 않은 `<img>` 에 원본 파일로 가는 링크를 씌운다.
격자와 제목의 계열 아이콘이 그래서 누르면 `icon-post-light-image.png` 파일 화면으로 갔고 사용자가 불편하다고 했다.
아이콘은 옆 계열 이름과 같은 링크 안에, 제목 아이콘은 제 자리로 가는 링크 안에 둔다.

**README 와 카테고리 문서 안에서 뛰는 링크는 `#user-content-` 로 시작한다.** 문서를 넘어가는
링크도 같다(`docs/themes/pattern.md#user-content-먹빛-민트`). GitHub 은 문서 안의 id 앞에
`user-content-` 를 붙여 두고, 링크를 누르면 자바스크립트가 그 접두사를 채워 찾아간다.
그 스크립트가 안 도는 자리가 있다 — 모바일 브라우저와 GitHub 앱. 거기서는 `#먹빛-민트` 를
눌러도 아무 일도 안 난다. 처음부터 진짜 id 를 가리키면 브라우저가 알아서 찾아간다.

## 커스텀 테마 제작

배경과 말풍선을 서로 다른 테마에서 하나씩 골라 섞는 길이 셋이다. **부르는 이름은
`커스텀 테마 제작` 하나로 통일한다** — `섞어 만들기` 처럼 자리마다 다르게 부르면 같은 것인 줄 모른다.

| 어디서 | 무엇이 나오나 | 어떻게 |
|---|---|---|
| `docs/make.html` (Pages) | iOS `.ktheme` | 폰 브라우저가 `files/` 의 `.ktheme` 두 벌을 받아 섞는다 |
| `.github/workflows/custom.yml` | `.ktheme` + `.apk` + 미리보기 | 러너가 `tools/mix.py` 와 `build-one.ps1` 을 돌린다 |
| `tools/mix.py` | `build-src/custom/` | 로컬 |

**안드로이드는 브라우저에서 못 만든다.** APK 는 서명이 있어야 하고 개인키가 필요하다.
iOS `.ktheme` 은 그냥 zip 이라 브라우저에서 만들 수 있다. 이 비대칭은 없앨 수 없으니
페이지에 적어서 Actions 쪽으로 보낸다.

**웹 쪽은 양쪽 다 배경 그림이 있는 테마만 고르게 한다**(`preview.write_mix_manifest`).
말풍선 쪽 `.ktheme` 을 통째로 가져와 배경 여섯 장(채팅방·목록·잠금화면 × @2x/@3x)만
갈아끼우면 끝이라, CSS 는 테마 이름과 테마 ID 두 줄만 고친다. 한쪽에 배경이 없으면
`-ios-background-image` 줄을 넣거나 빼야 하는데 그건 브라우저에서 할 일이 아니다.
배경 없는 테마가 좋으면 그 테마를 그냥 받으면 되므로 잃는 것도 없다.

**테마 ID 를 반드시 바꾼다.** 안 바꾸면 폰이 원래 테마를 덮어쓴다. 조합마다 다른 ID 를 주면
원본과 나란히 깔린다.

**미리보기는 방금 받은 그 `.ktheme` 의 CSS 와 그림으로만 그린다.** 팔레트 표를 자바스크립트로
옮겨 적지 않는다 — 두 벌이 되는 순간 한쪽이 틀리기 시작한다. cap 과 `edgeinsets` 도 CSS 에서
읽고, 말풍선은 카톡처럼 cap 자리의 한 줄만 늘인다(`stretch()`).

**불빛이 구워진 말풍선은 말풍선 목록에서 뺀다.** `firelight` 를 쓴 캠핑·도트 모험 어두움+배경이
그렇다. 불 없는 배경에 얹으면 까닭 없는 얼룩이 된다. `mix.py` 도 같은 것을 턴다.

**zip 을 손으로 읽고 쓴다.** 라이브러리를 들이지 않는다 — 필요한 것은 바이트 몇 덩어리뿐이고,
그림은 압축을 풀지도 않고 압축된 채로 옮겨 담는다. 헤더 자리를 한 칸만 틀려도 폰에 넣기
전까지 모르므로 **고치면 `node tools/check-make.mjs` 를 돌린다** — `dist/iOS` 의 실제 테마
두 벌로 섞어 보고 나온 zip 을 다시 읽어 확인한다. 점검은 페이지의 코드를 주석 구역으로
잘라 와서 쓴다. 베껴 두면 두 벌이 되어 한쪽만 고쳐진다.

**`docs/` 에 파일을 더하면 `pages.yml` 의 `cp` 줄에도 적는다.** 사이트는 `docs/` 를 통째로
옮기는 게 아니라 이름을 하나씩 적어 옮긴다.

**README 에서는 `받는 법` 다음, 테마 목록 앞에 둔다.** 테마 목록 뒤에 두었더니 계열
예순넷을 지나야 나와서 있는 줄 모른다. 받는 법 바로 뒤가 "고르는 것 말고 만들 수도 있다" 를
알리기 제일 좋은 자리다.

**인앱 브라우저에서 바로 설치하는 길은 아직 없다.** 카톡이 가로채는 것은 주소로 된
`.ktheme` 뿐인데 브라우저가 만든 파일은 `blob:` 주소라 알아볼 것이 없고, 조합이 만 벌이
넘어 미리 만들어 둘 수도 없다. 서비스 워커가 가짜 주소를 가로채 응답하는 길이 있는데,
카톡이 그 주소를 웹뷰로 받으면 되고 앱이 직접 받으면 안 된다 — `docs/probe.html` 이 그걸
폰에서 찍어 보는 실험이다. 판가름 나면 실험 파일 둘을 지운다.

## 릴리스 규칙

**릴리스마다 모든 테마의 `.ktheme` 과 `.apk` 를 전부 자산으로 붙인다.** 한쪽만 올리지 않는다.
받는 사람은 저장소가 아니라 릴리스 자산에서 받는다 — 그래서 `dist/` 는 커밋하지 않는다.

```powershell
powershell -ExecutionPolicy Bypass -File release.ps1 -Version 0.4 -NotesFile notes.md
```

`release.ps1` 이 하는 일과 그 이유:

1. `tools/themes.py` 의 `VERSION` 과 요청 버전이 같은지 본다. 다르면 멈춘다.
2. 워킹 트리가 깨끗한지, 태그가 이미 있는지 본다. 중간 상태로 릴리스가 나가는 걸 막는다.
3. `build.ps1` 로 전부 빌드한다.
4. 태그를 밀고 `dist/` 의 파일을 전부 자산으로 붙인다.

**자산 이름에는 버전을 넣지 않는다** (`inkmint01.apk`). README 가
`releases/latest/download/` 로 바로 거는데, 이름에 버전이 들어가면 릴리스마다 링크가 깨진다.
버전은 태그와 릴리스 제목이 갖는다.

**품질 개선은 패치 번호로 올린다.** `0.13` → `0.13.1` → `0.13.2`.
같은 것을 더 낫게 만든 작업은 새 버전이 아니다. 마이너(`0.14`)는 테마를 더하거나
할 수 있는 것이 늘었을 때만 올린다. 이미 나간 릴리스를 덮어쓰지 않는다 —
받아 간 사람이 무엇을 받았는지 알 수 없게 된다.

`versionCode` 는 패치까지 반영한다(`mixed09` + `0.13.1` → `901301`).
자리를 넉넉히 잡아둬서 패치를 계속 올려도 번호가 내려가지 않는다.

**릴리스 노트는 직접 쓴다.** 자동 생성에 맡기지 않는다 — main 에 바로 커밋하는 프로젝트라
자동 노트는 링크 한 줄만 남고 비어버린다. 확인 못 한 것도 적는다.

## 받는 길

**iOS 링크는 Pages 의 `docs/share.html` 을 거치고, 안드로이드는 릴리스 자산을 바로
가리킨다.** 비대칭이지만 의도한 것이다 — APK 는 카톡이 아니라 시스템이 설치한다.

**브라우저마다 제일 짧은 길이 다르다.** 그래서 링크를 늘리지 않고 페이지가 고르게 했다.
README 배치 규칙상 테마마다 링크를 늘릴 수도 없다.
인앱 브라우저의 바로 설치와 사파리의 내려받기는 실물로 확인했다. 깃허브 앱은 확인 전이다.

| 어디서 | 어떻게 | 결과 |
|---|---|---|
| 카카오톡 인앱 브라우저 | `files/` 파일 주소로 넘긴다 | 카톡이 가로채 **바로 설치**. 제일 짧다 |
| 사파리 · 깃허브 앱 | 공유 시트를 띄운다 | 카톡으로 보내고 채팅방에서 눌러 설치 |
| 데스크톱 | 파일로 내려받는다 | |

핵심은 헤더 하나다. **릴리스 자산은 `Content-Disposition: attachment` 를 달고 나온다.**
그게 붙으면 브라우저는 내려받는 것 말고 아무것도 못 한다. **Pages 의 같은 파일에는 그 헤더가
없어서** 카카오톡이 가로챌 여지가 남는다. **그래서 iOS 쪽에 릴리스 자산 주소를 쓰면 안 된다** —
이게 "받는 사람은 릴리스 자산에서 받는다" 규칙의 유일한 예외다.

**사파리는 그래도 안 된다.** 그 헤더가 없어도 `.ktheme` 을 그냥 내려받는다. 카카오 공식
안내는 사파리에서도 〈카카오톡으로 열기〉 가 뜬다고 적고 있지만 지금 iOS 에서는 아니다.
문서를 믿고 직링크로 바꾸지 말 것. 확인한 결과가 위 표다.

**인앱 브라우저는 UA 의 `KAKAOTALK` 으로 가린다.** 다른 사이트의 링크를 타고 들어오면
그 표시가 빠질 수 있는데, 못 알아봐도 괜찮게 짜여 있다 — 그때는 공유 갈래로 갔다가
파일 공유가 없어서 결국 파일 주소로 떨어지고, 같은 자리로 돌아온다.

**공유 시트에서는 링크 보내기가 기본이다.** 채팅방에서 그 링크를 누르면 인앱 브라우저가
열려 첫 번째 길을 탄다. 파일 보내기보다 짧고, 내려받을 것이 없어 기다림도 없다.
파일 보내기는 방에 파일 자체를 남기고 싶을 때 쓴다.

**공유 페이지가 읽는 바이트도 사이트 안에서 온다.** 릴리스 자산에는
`Access-Control-Allow-Origin` 이 없어서 `fetch` 로 못 읽는다. `api.github.com` 입구에는
붙어 있지만 최종 목적지인 `release-assets.githubusercontent.com` 에 없고,
`fetch` 는 리다이렉트 매 홉을 검사하므로 거기서 막힌다. 그래서 Pages 워크플로가
릴리스에서 `.ktheme` 을 받아 사이트에 한 벌 둔다. **저장소에 커밋하는 게 아니다** —
`dist/` 를 커밋하지 않는다는 규칙은 그대로다.

**사이트는 저장소 구조를 그대로 옮긴다.** `docs/` 는 `docs/` 로, `assets/` 는 `assets/` 로.
그래야 `docs/index.html` 이 쓰는 `../assets/` 경로가 로컬에서 파일로 열 때와 사이트에서
똑같이 동작한다. 경로를 두 벌 관리하지 않으려는 것이다. 내려받은 테마는 `files/` 에 들어간다.

**릴리스를 내면 사이트도 따라간다.** 워크플로가 `release: published` 에 걸려 있어서
새 릴리스의 자산으로 갱신된다. 파일 이름을 바꾸는 릴리스를 낼 때는 README 링크와
사이트의 파일 이름이 같이 바뀌므로 따로 할 일이 없다.

**공유 단추가 눌린 뒤에 파일을 받아오면 안 된다.** `share()` 는 탭이 만든 일시적 활성화를
소비하는데 그 창이 짧다. 누른 뒤에 내려받기 시작하면 느린 회선에서 `NotAllowedError` 로
떨어진다. 페이지를 열 때 미리 받고, 다 받기 전까지 단추를 잠가 둔다.

## 다른 세션이 같은 저장소를 만지고 있을 때

**워킹 트리를 건드리지 않는다.** 별도 작업 폴더에서 하고, 갈아끼울 준비까지만 하고 멈춘다.

도는지 아닌지는 시작 전에 본다. 하나라도 걸리면 도는 것으로 친다.

- `git status` 에 **내가 만들지 않은 변경**이 있다
- 파이썬이나 `aapt2`·`java` 프로세스가 방금 떠 있다
- `.git/index.lock` 이 있다

같은 트리에서 같이 작업하면 두 가지가 깨진다. 저쪽의 `git add -A` 가 내 변경을
자기 커밋에 쓸어 담고, `build-src/` 는 테마마다 지워지고 다시 만들어지므로
양쪽 빌드가 서로의 중간 산출물을 읽는다.

```bash
git worktree add <임시경로> -b wip/<이름> HEAD
```

작업 폴더는 `git worktree` 로 만든다. 복사본과 달리 커밋을 남길 수 있어서
나중에 합칠 때 무엇을 바꿨는지가 그대로 남는다. **HEAD 에서 딴다** —
저쪽이 고치는 중인 파일은 반쯤 쓰여 있을 수 있다.

끝나면 거기서 커밋만 해두고 사용자에게 알린다. 합치는 것은 저쪽 작업이 끝난 뒤다.

```bash
git merge wip/<이름>        # 저쪽이 끝난 뒤
git worktree remove <임시경로>
git branch -d wip/<이름>
```

## 저장소에 넣지 않는 것

`.gitignore` 에 있지만 이유를 적어둔다. 파일은 로컬에 그대로 둔다 — 지우는 게 아니다.

- `dist/`, `build-src/`, `build-tmp/` — 생성물. 원본은 팔레트 표뿐이다.
- `theme.keystore` — 서명 키. 공개 저장소에 올라가면 누구나 같은 서명의 APK 를 만들 수 있다.
  잃어버리면 같은 패키지의 새 버전을 덮어 설치할 수 없다. 폰에서 지우고 새로 깔면 된다.

## 리소스 이름

블록·속성·리소스 이름은 **카카오 공식 테마 가이드에서만** 가져온다.
남의 테마를 뜯어서 베이스로 쓰지 않는다 — 배포 조건이 대개 재배포 금지다.

- [iOS 가이드](https://t1.kakaocdn.net/kakaocorp/Service/Theme/KakaoTalk/9.2.5_UserThemeGuide_iOS.pdf)
- [Android 가이드](https://t1.kakaocdn.net/kakaocorp/Service/Theme/KakaoTalk/9.2.5_UserThemeGuide_Android.pdf)

가이드 PDF 는 폰트 인코딩이 밀려 있어서 그냥 열면 안 읽힌다. 글자 코드에 +31 하면
영문이 나온다(`0x42`~`0x5B` → `a`~`z`, `0x40` → `_`, `0x0E` → `-`, `0x0F` → `.`,
`0x11`~`0x1A` → `0`~`9`). 한글은 커스텀 인코딩이라 복구가 안 된다. 이름만 뽑는 용도로 쓴다.

**규격 전체는 `docs/spec.md` 에 정리해뒀다.** 0.23 부터 가이드에 적힌 자리를
전부 쓴다 — 색, 말풍선, 배경 셋, 탭 아이콘, 기본 프로필, 친구추가 단추,
잠금화면 동그라미 여덟 장, 키패드 눌림, 탭바 배경.

**이름을 지어내지 않는다.** 가이드에 없는 속성은 쓰지 않는다 — iOS 탭바에
이미지를 넣는 속성이 없어서 거기만 색으로 둔 것이 그런 경우다. 카톡은 모르는
이름을 조용히 무시하므로, 지어낸 이름은 폰에서도 안 걸리고 그냥 안 먹는다.

## 함정

- **`.ktheme` 을 안드로이드에 넣을 수 없고 반대도 안 된다.** 포맷이 완전히 다르다.
- **iOS 말풍선은 색상값으로 못 바꾼다.** `MessageCellStyle` 에 `background-color` 가 없다. PNG 를 그려야 한다.
- **실행화면(스플래시)은 안드로이드만 된다** (`theme_splash_image`). iOS 규격에는 블록 자체가 없다.
- 안드로이드 테마 APK 는 권한을 하나도 요구하지 않는다. 설치할 때 권한 안내가 뜨면 잘못된 것이다.
- gradle 은 쓰지 않는다. 코드 없는 리소스 전용 APK 라 `aapt2` → `zipalign` → `apksigner` 면 끝난다.
- **네이티브 exe 의 stderr 를 `2>&1` 로 받지 않는다.** PowerShell 5.1 은 그렇게 받으면 종료코드가
  0이어도 `NativeCommandError` 로 승격시켜 빌드가 실패한 것처럼 보인다. apksigner 가 여기 걸렸다.
- **한글을 네이티브 프로그램의 명령줄 인자로 넘기지 않는다.** PowerShell 이 시스템 코드페이지로
  인코딩해서 깨진다. `gh release create --title "카카오톡 테마 0.3"` 이 "移댁뭅?ㅽ넚" 이 됐다.
  파일을 거쳐 보낸다 — `git tag -F`, `gh api --input <json>`. 표준입력이나 `--notes-file` 로
  들어가는 내용은 멀쩡하다. 인자만 문제다.
- **파이썬 출력은 UTF-8 로 고정한다.** 윈도우 기본이 cp949 라 한글이 깨진다.
  `sys.stdout.reconfigure(encoding='utf-8')` 을 도구 맨 위에 둔다.
  PowerShell 쪽은 `[Console]::OutputEncoding` 을 UTF-8 로 둔다.
- **PIL 의 `Image.effect_noise` 는 씨앗을 못 준다.** 파이썬 `random` 이 아니라 C 쪽 난수를
  쓰고 씨앗을 줄 자리가 없다. 밤하늘의 밴딩 깨는 잡음이 여기 걸려서, 별은 키에서 나온
  씨앗으로 고정됐는데 잡음만 매번 달라졌다. 심야 그림 여덟 장이 미리보기를 돌릴 때마다
  diff 에 떠 있었고, 아무도 고친 적 없는 파일이라 그게 무엇인지 한참 안 보였다.
  그림에 난수를 쓸 때는 `random.Random(씨앗)` 으로 직접 뽑는다.
  **절반 크기로 만들어 키우지 않는다** — 키울 때 생긴 두 픽셀짜리 알갱이가 흐림을 견뎌
  PNG 가 두 배가 된다(108KB → 197KB). 잡음은 목표 크기로 그대로 뽑는다.
- **RGBA 이미지에 알파를 섞어 그리면 PIL 은 섞지 않고 덮어쓴다.** `ImageDraw.Draw(img, 'RGBA')`
  는 바탕이 RGB 일 때만 알파를 섞는다. RGBA 바탕에서는 알파까지 통째로 바뀌고, 나중에 RGB 로
  바꿀 때 그 색이 원색으로 나온다. 나무결에서 알파 15 로 칠한 판자가 원색 띠로 나왔다.
  바탕을 RGB 로 두고 그리거나, 따로 만든 판에 그려 `alpha_composite` 로 얹는다.
- **RGB 로 색을 밝히면 진한 색이 회색이 된다.** 채널마다 255 쪽으로 당기는 `lighten()` 은
  어두울수록 세 채널이 나란히 올라가 채도를 잃는다. 레드의 받은 말풍선(`#3B0B16`)은
  채도 0.81 이 0.09 로 떨어져 글로우 테두리가 잿빛이었다 — 빨간 테마인데 테두리만
  회색이면 빛이 아니라 덧그린 선으로 보인다. 밝은 파스텔은 원래 채도가 낮아 티가 안 나서,
  어떤 계열은 예쁘고 어떤 계열은 아니었다. 빛을 그릴 때는 `glow_tint()` 처럼 색상을 두고
  명도만 올린다. 순백을 진하게 얹는 것도 같은 문제다 — 아래 깔린 빛을 덮어 흰 선이 된다.
- **안드로이드 `versionCode` 를 목록 순서로 매기지 않는다.** 테마 순서를 바꾸면 번호가
  내려가는 테마가 생기고, 안드로이드는 그걸 다운그레이드로 보고 설치를 거부한다.
  키 끝의 번호와 테마 버전으로 만든다 — `mixed09` + `0.8` → `9008`.
- PowerShell 스크립트는 **UTF-8 BOM** 으로 저장한다. BOM 이 없으면 5.1 이 ANSI 로 읽어 한글이 깨진다.
- **릴리스 자산은 자바스크립트로 못 읽는다.** CORS 헤더가 없다. 브라우저에서 파일 내용이
  필요하면 Pages 사이트 안(같은 출처)이나 `raw.githubusercontent.com` 에서 읽어야 한다.
- **릴리스 자산 링크로는 `.ktheme` 을 카톡에 바로 못 넘긴다.** `Content-Disposition: attachment`
  가 붙어 나와서 브라우저가 내려받기만 한다. Pages 의 같은 파일에는 그 헤더가 없다.
- **안드로이드는 `.apk` 를 웹 공유로 못 보낸다.** 크롬의 공유 API 는 확장자 허용목록 방식이라
  실행 파일류가 아예 빠져 있다. 공유 방식은 iOS 전용으로 생각한다.
