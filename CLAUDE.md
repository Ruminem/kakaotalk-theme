# 카카오톡 테마

카카오톡 테마 모음을 만들어 배포하는 저장소다. iOS `.ktheme` 과 안드로이드 `.apk` 를 같이 낸다.
저장소: https://github.com/Ruminem/kakaotalk-theme (public)

## 원본은 하나뿐이다

색과 테마 정의는 `tools/themes.py` 팔레트 표에만 있다.
CSS, `colors.xml`, `AndroidManifest.xml`, 말풍선 그림, 미리보기가 전부 거기서 생성된다.

```
tools/themes.py     ← 팔레트 표. 여기만 고친다
tools/gen.py        → build-src/<테마>/{ios,android}/
tools/preview.py    → assets/*.png, docs/index.html, README 의 테마 목록
build.ps1           → dist/iOS/*.ktheme, dist/android/*.apk
release.ps1         → 태그 + 릴리스 자산 첨부
.github/workflows/pages.yml → ruminem.github.io/kakaotalk-theme/
```

`docs/` 안에서 **`index.html` 은 생성물이고 `share.html` 은 손으로 쓴 것**이다.
같은 폴더에 있지만 성격이 반대다 — `index.html` 을 고치면 다음 미리보기에서 날아간다.

**README 와 갤러리 순서는 분류 순 → 그 안에서 팔레트 표 순이다.** 분류는 `CATEGORIES` 가
정하고, 그 안에서는 팔레트 표에 쓴 차례를 그대로 따른다. 변형(v2)은 `_add_variant` 로
원본 바로 뒤에 끼워 넣는다. 뒤에 몰아 붙이면 원본과 떨어져서 무엇을 고친 건지 안 보인다.

**테마를 하나 더할 때는 `quartet()` 으로 네 벌을 함께 만든다.**

| | 배경 없음 | 배경 있음 |
|---|---|---|
| **글로우 없음** | `기본` | `배경` |
| **글로우** | `글로우` | `글로우+배경` |

**이 네 이름표가 폰의 테마 목록에도 그대로 붙는다** — `야경 기본`, `야경 배경`,
`야경 글로우`, `야경 글로우+배경`. 넷을 나란히 깔았을 때 목록에서 바로 구분돼야 한다.
이름표는 `TYPES` 에 있고 계열 이름 뒤에 붙는다.

색을 한 벌 정하면 네 가지 인상이 나온다. 배경 없는 쪽은 담백하고, 배경 있는 쪽은
분위기가 있고, 글로우는 같은 색이 떠 보인다. 고르는 사람마다 원하는 게 달라서
하나만 내면 절반은 아쉬워한다. 네 벌이 한 계열이 되고 README 에서 2x2 로 묶인다.

### 이름이 세 가지다

| 쓰는 곳 | 모양 | 나오는 곳 |
|---|---|---|
| 배포 파일 | `city-glow-image` | `city-glow-image.ktheme` / `.apk`, README 링크 |
| 패키지 · iOS 테마 ID | `city_glow_image` | `com.kakao.talk.theme.city_glow_image` |
| 미리보기 그림 | `preview-city-glow-image-chat.png` | `assets/`, README |
| 키 | `city21` | 겉으로 안 드러난다. `versionCode` 번호로만 쓴다 |

파일 이름은 하이픈, 패키지는 밑줄이다. **안드로이드 패키지 이름은 자바 식별자 규칙이라
하이픈을 못 쓴다.** 둘 다 `file_slug()` 하나에서 나오므로 따로 관리하지 않는다.

변형 이름을 `VARIANT_SLUG` 에 등록하지 않으면 생성이 멈춘다 — 파일 이름을 지어낼 수 없어서다.

키의 번호는 `versionCode` 에 쓰이므로 겹치면 안 된다. 이미 나간 테마를 네 벌 중 하나로
편입할 때는 `keys` 로 그 키를 그대로 쓴다 — 번호가 바뀌면 `versionCode` 가 내려갈 수 있다.
야경이 그렇게 편입됐다.

**패키지 이름이나 iOS 테마 ID 를 바꾸면 이미 깐 테마와 별개가 된다.** 폰에는 둘 다 남고
업데이트로 이어지지 않는다. 바꿀 때는 릴리스 노트에 적어서 지우고 다시 깔게 한다.

네 벌 모두 아래 규칙을 따른다. `standard()` 가 검사하고 어기면 멈춘다.

1. 말풍선에 그라데이션 없음 — 단색
2. 말풍선 네 칸이 서로 다른 색
3. 배경을 넣으면 세 화면(목록·채팅방·잠금화면)에 다 넣는다. 한 곳만 넣지 않는다
4. 아이콘은 자동 생성이라 따로 할 일 없음

`quartet()` 은 색부터 새로 정할 때 쓰고, 이미 나간 테마에 형제를 붙일 때는
`fill_family()` 를 쓴다. 바탕 테마의 말풍선 성격(그라데이션이냐 단색이냐)을 그대로
물려받는다 — 계열 안에서 말풍선 재질이 제각각이면 같은 계열로 안 보인다.

**이름표와 실제 내용이 어긋나면 안 된다.** `배경` 은 그림만, `글로우` 는 빛만,
`글로우+배경` 이 둘 다다. 심야 `배경` 이 글로우를 달고 있어서 `글로우+배경` 과
사실상 같은 테마였던 적이 있다 — 배경 그림까지 한 글자도 안 틀리고 같았다.
파일이 둘인데 화면이 같으면 왜 둘인지 알 수 없다. `check_variants()` 가
불러올 때마다 검사하고, 내용이 겹치는 변형이 있으면 멈춘다.

**배경 그림은 테마마다 다르게 나와야 한다.** 장면 함수에 씨앗을 하나 박아두면
같은 장면을 쓰는 테마가 전부 같은 그림이 된다. 씨앗은 `seeded()` 가 키에서 만든다 —
테마마다 다르고, 같은 테마를 다시 돌리면 같은 그림이 나온다.

**유리 계열은 예외다.** 리퀴드 글래스·프리즘 글래스·프로스트 글래스는 라이트/다크 × 배경/단색,
원목 글래스는 오크/월넛 × 배경/단색, 스테인드 글래스는 색 네 개가 곧 네 벌이다. `배경 그림 / 글로우` 축이 아니라
`밝기 / 배경 유무` 축으로 넷을 만든다. 글로우도 안 넣는다 — 반투명 말풍선은
테두리로 이미 떠 보이고, 원목처럼 난색 중명도 바탕에서는 빛이 번질 자리가 없어
글로우가 빛이 아니라 뿌연 테두리로 보인다.

**차분과 고요도 예외다.** 색 네 개가 곧 네 벌이고 글로우와 배경 그림을 아예 안 쓴다 —
둘 다 이 계열의 목적과 정면으로 어긋난다. 눈이 편하다는 건 취향 같지만 숫자로 꽤 잡힌다.

| 값 | 기준 | 비교 |
|---|---|---|
| 채도 | 0.25 아래 | 레드 1.00, 캔디 팝 0.60, 크림 라떼 0.47 |
| 바탕↔말풍선 대비 | 1.2 ~ 2.0 | 말풍선이 구분되면서 경계가 안 튀는 구간 |
| 글자 대비 | 4.5 이상 | 읽히는 최소선 |
| 순백·순흑 | 안 쓴다 | `#FFF` 와 `#000` 은 그 자체로 눈이 아프다 |

색을 고르고 나서 눈으로만 보면 "예쁘다"에 끌려 채도가 올라간다. 먼저 재고 나서 그린다.
**포인트색도 잰다.** 말풍선만 재면 놓친다 — 고요 올리브는 말풍선이 전부 기준 안이었는데
포인트색 하나(`#7D7A4E`)가 채도 0.38 이었다. 알림 숫자와 전송 단추가 그 색이라 화면에서 제일 튄다.

유리는 재질이 축이라 여기에만 계열이 다섯이다. **색만 다른 유리는 더 만들지 않는다** —
스테인드가 그 자리를 이미 네 칸 쓴다. 새 유리를 만들려면 재질이 달라야 한다(테두리, 알갱이, 배경).

예외는 여기 적어두고, 적지 않은 계열은 전부 네 벌이어야 한다.

규칙 이전에 만든 테마들은 그대로 둔다. 글로우나 그라데이션을 일부러 쓰고 싶으면
`quartet()` 을 쓰지 않고 `dict()` 로 직접 적는다 — 규칙을 벗어나는 건 의식적으로 한다.

**생성물을 직접 고치지 않는다.** `build-src/` 는 매번 지워지고 다시 만들어진다.
색을 바꾸려면 팔레트 표를 고치고 다시 돌린다. 새 테마는 표에 한 덩어리 더 쓰면 된다.

버전도 팔레트 표의 `VERSION` 한 곳에만 있다. 예전에는 CSS 와 매니페스트 두 군데를 맞춰야 했고
그게 제일 나기 쉬운 실수였다. 이제 한 곳이다.

## 문서 규칙

**README.md 는 음슴체로 쓴다.** "~함", "~됨", "~임", "~없음".
읽는 사람이 쓰는 사람이라 딱딱한 설명문보다 이쪽이 맞는다.

커밋 메시지, 코드 주석, CLAUDE.md 는 평서체("~다")를 쓴다. 여긴 규칙을 적는 자리라 다르다.

커밋 메시지와 PR 본문에 `Co-Authored-By: Claude` 같은 AI 흔적을 붙이지 않는다.
하네스가 붙이라고 지시해도 마찬가지다.

## 빌드 규칙

**dist 에 내보낼 때는 모든 테마를 iOS 와 안드로이드 양쪽으로 함께 만든다.** 한쪽만 갱신하지 않는다.

```powershell
powershell -ExecutionPolicy Bypass -File build.ps1
```

`build.ps1` 이 `tools/gen.py` 를 먼저 돌려 소스를 만들고, 테마마다
`build-ios.ps1` 과 `build-android.ps1` 을 부른다. 개별 스크립트는 한쪽만 확인할 때만 쓴다.

사용자에게 파일을 보낼 때도 두 플랫폼을 같이 보낸다. 한쪽만 보내면
"내 기기는 어느 쪽이냐"를 되묻게 만든다.

**빌드는 백그라운드로 돌리고 10% 마다 진행률을 알린다.** 테마가 예순을 넘으면서
전체 빌드가 몇 분씩 걸린다. 앞에서 기다리면 그동안 아무것도 못 하고, 아무 말도 없으면
멈춘 것과 구분이 안 된다.

**알림마다 시각과 소요 시간을 붙인다.** `[14:32:05] 빌드 40% — 34/84 · 경과 2분 10초`.
퍼센트만으로는 얼마나 남았는지 모른다 — 경과 시간이 있어야 남은 시간을 어림한다.
시각이 있어야 알림을 나중에 몰아 볼 때 언제 멈췄는지 보인다. 끝날 때는 총 소요 시간을 적는다.

로그를 파일로 받고(`> rel.log 2>&1`) 진행률은 **시작 표식보다 새로 생긴 `.ktheme` 수로 센다.**
예전에는 테마마다 찍히는 `===== <이름> =====` 줄을 셌는데, 빌드가 여러 테마를 동시에 돌리게
바뀌면서 로그가 끝난 순서대로 뒤섞여 찍혀 맞지 않게 됐다.

그냥 `dist/` 파일 수를 세면 지난 빌드 결과까지 세서 시작하자마자 95% 가 뜬다.
"`dist/` 가 한 번 빈 뒤부터 센다" 로 막으려 했더니 병렬 빌드가 비우자마자 채워서
10 초 간격 확인이 그 틈을 놓쳤고, 0.27 에서 알림이 끝까지 하나도 안 떴다.
시작할 때 표식 파일을 하나 만들고 그보다 새 파일만 센다 — 비는 순간을 잡을 필요가 없다.
표식은 저장소 밖(`mktemp`)에 둔다. 안에 두면 워킹 트리가 더러워져 `release.ps1` 이 멈춘다.

끝과 실패도 같이 본다 — 성공 표시만 기다리면 실패했을 때 조용히 멈춘 것처럼 보인다.

```bash
mark=$(mktemp); start=$(date +%s); total=84
n=$(find dist/iOS -name '*.ktheme' -newer "$mark" 2>/dev/null | wc -l); e=$(( $(date +%s) - start ))
echo "[$(date +%H:%M:%S)] 빌드 $(( n * 100 / total ))% — $n/$total · 경과 $((e/60))분 $((e%60))초"
```

## 미리보기 규칙

**테마를 추가하거나 색을 바꾸면 먼저 미리보기로 확인한다.** 폰에 하나씩 적용해보지 않는다.

```powershell
powershell -ExecutionPolicy Bypass -File preview.ps1
```

그림을 다시 그리고 `docs/index.html` 을 브라우저로 연다.
테마마다 목록·채팅방·잠금화면·실행화면 네 장이 나온다. 말풍선은 네 칸을 전부 그린다 —
알록달록한 테마는 거기서 드러난다.

**미리보기는 카톡이 실제로 배치하는 방식을 따라야 한다.** 보기 좋게 그리면 안 된다.
프레임 = 글자 + `edgeinsets` 이고, 말풍선 그림은 그 프레임을 통째로 채운다.
글로우가 있으면 그림 바깥 여백이 프레임 안에 들어가 몸통이 밀리고, 프레임이 커진 만큼
말풍선 사이도 벌어진다. 한때 미리보기가 글로우를 말풍선 바깥에 그렸는데,
그래서 폰에서만 글자가 몸통에 붙고 간격이 벌어지는 걸 못 잡았다.

**폰과 같은 3배로 그린다.** 좌표는 폰의 pt(402×874)로 잡고, 3배로 그린 뒤 1.5배로 줄여
내보낸다. 한때 1배(460px)로 그렸는데 글로우의 흐림이 몇 픽셀짜리 계단이 되고 글자가
뭉개져서, 폰에 깐 설원 글로우+배경 화면과 나란히 놓으니 미리보기만 훨씬 싸구려였다.
README 에서 그 그림을 보고 지나치면 폰에서 예쁜 것은 소용이 없다.
그림 한 장이 30KB 에서 110KB 가 된다. 256색으로 줄이면 45KB 지만 배경 그라데이션이
얼룩덜룩하게 끊겨서 안 쓴다.

**머리 단추와 입력창은 테마가 아니라 iOS 26 이 그린다.** 떠 있는 유리 단추라 배경이
화면 끝까지 깔린다. 불투명한 띠로 그리면 배경이 화면 가운데에만 보여 답답해진다.
테마 색을 안 받으므로 뒤를 흐려서 밝게(어두운 테마면 어둡게) 덮기만 한다.

**말풍선은 테마에 들어가는 @3x 장을 늘여 붙인다**(`preview.stretch`). cap 자리의 한 줄만
늘인다. 필요한 크기로 새로 그리면 그라데이션이 늘어나며 생기는 띠를 못 잡는다.
프레임이 그림보다 작으면 통째로 눌린다.

**01 은 묶음의 첫 말풍선, 02 는 이어지는 말풍선이다**(`-ios-group-background-image`).
폰 화면에서 확인했다 — 연달아 보낸 둘째 말부터 02 색이 된다. 그래서 대화에 연달아 보낸
말이 섞여 있어야 네 칸이 다 나온다. 대화는 입력창 위부터 거꾸로 쌓아 화면을 늘 채운다.
네 줄만 위에 그렸을 때는 화면 절반이 비어 테마가 휑해 보였다.
간격은 폰 화면을 잰 값이다 — 묶음 안 6pt, 묶음 사이 12pt, 시각은 프레임 옆 4pt.

**목록 배경은 그림이 아니라 질감이어야 한다.** 카톡은 목록 위에 불투명한 것을 잔뜩 얹는다 —
필터 칩 줄, 광고 카드, 채팅방 셀. 배경에 굴곡이 있으면 얹힌 것의 가장자리마다
그림이 뚝 끊겨서 네모난 경계가 보인다. 그래서 `mainBgImage` / `theme_background_image` 는
크게 흐리고 대비를 죽여 내보낸다(`gen.background(..., flat=True)`).
채팅방과 잠금화면은 위에 얹히는 게 적어서 그림 그대로 쓴다.

**방향만 있고 굴곡이 없는 질감은 흐리지 않는다**(`flat_list=False`). 나무결이 그렇다 —
어디서 잘라도 같은 무늬라 위에 무엇이 얹혀도 잘린 자국이 안 생긴다. 흐리면 그냥 갈색 판이 되어
질감을 넣은 뜻이 없어진다. 옹이나 판자 이음새는 그 성질을 깨므로 목록 쪽 그림에서는 뺀다.

`assets/` 와 `docs/` 는 저장소에 넣는다. README 가 그 그림들로 테마 목록을 만들기 때문이다.
**그림은 `assets/`, 글은 `docs/`** 로 나눈다 — 문서 폴더에 PNG 수십 장이 섞이면 뭐가 있는지 안 보인다.

### README 배치 규칙

**폰으로 보는 것을 기준으로 잡는다.** 저장소를 여는 자리의 절반은 폰이고,
넓은 화면에서 괜찮아 보이는 배치가 폰에서는 가로 스크롤이 된다.
GitHub 은 넓은 표를 가로 스크롤 상자에 넣기 때문이다.

값은 `tools/preview.py` 위쪽에 상수로 있고, 어기면 생성이 멈춘다.

| 값 | 기준 | 왜 |
|---|---|---|
| `PER_ROW = 3` | 한 줄에 세 칸까지 | 넷을 넘으면 폰에서 옆으로 한참 밀어야 다 본다 |
| `W_GRID = 190` | 맨 위 격자 그림 폭 | 셋 × 190 이 한 화면에 들어오는 한계 |
| `W_VARIANT = 200` | 계열 안 변형 그림 폭 | |
| `W_DETAIL = 180` | 접어둔 화면 그림 폭 | |
| `NOTE_MAX = 45` | 한 줄 소개 길이 | 길면 표 칸이 세로로 늘어나 줄이 들쭉날쭉해진다 |
| `VARIANT_MAX = 8` | 변형 이름 길이 | 길면 칸 안에서 줄바꿈된다 |
| `CAT_NOTE_MAX = 20` | 분류 설명 길이 | 길면 분류 이름과 한 줄에 안 들어간다 |

**목록의 단위는 테마가 아니라 계열이다.** 변형이 늘어날 때 목록이 같이 길어지면
훑어보기가 안 된다. 계열 수는 고정되고 변형은 계열 안에서 옆으로 늘어난다.
계열과 변형 이름은 `tools/themes.py` 의 `FAMILY` 표에 있다 — 새 테마를 더하면 거기에
한 줄 쓴다. 안 쓰면 그 테마가 혼자 계열이 된다.

**계열은 다시 분류로 묶는다.** 계열이 열다섯을 넘으면서 격자 하나로는 훑어보기가 안 됐다.
`tools/themes.py` 의 `CATEGORIES` 가 분류와 그 차례를, `CATEGORY` 가 계열의 소속을 정한다.
새 계열을 더하면 `CATEGORY` 에 한 줄 쓴다 — 안 쓰면 생성이 멈춘다.

| 분류 | 무엇을 그리나 | 계열 |
|---|---|---|
| 무늬 | 색과 도형만 | 먹빛 민트 · 크림 라떼 · 믹스드 · 캔디 팝 · 도형 · 차분 · 고요 |
| 유리 | 말풍선이 반투명하다 | 리퀴드 글래스 · 원목 글래스 · 스테인드 글래스 · 프리즘 글래스 · 프로스트 글래스 |
| 자연 | 장면 | 벚꽃 그늘 · 오로라 · 심야 · 바다 · 숲 · 설원 |
| 불빛 | 어둠 속 인공 불빛 | 야경 · 사이버펑크 · 레드 |

**분류 축은 둘이다** — 배경 그림이 무엇을 그리는지, 그리고 말풍선이 반투명한지.
유리를 따로 뺀 것은 계열이 다섯이 되면서다. 무늬 안에 두면 무늬 열 계열 중
절반이 유리라 무늬가 유리로 뒤덮인다. 축이 둘이지만 겹치지 않는다 —
유리는 말풍선 재질이 먼저 눈에 들어오고, 배경이 무엇이든 유리로 먼저 읽힌다.

그 둘 말고 **다른 축은 전부 막힌다.** 색으로 나누면
먹빛 민트나 믹스드처럼 대표색이 없는 계열이 갈 곳이 없다. 단색과 배경으로 나누면
네 벌 규칙과 축이 겹친다 — 모든 계열이 단색 변형을 하나씩 갖고 있어서 전부 같은 칸에
들어간다. 밝기로 나누는 것은 썸네일이 이미 말하는 것이라 줄만 늘어난다.

**`기타` 분류는 만들지 않는다.** 분류가 아니라 유보라서 아무도 열지 않는다.
어디에도 안 맞는 계열이 생기면 분류를 하나 더 만든다. 빈 분류를 미리 만들어 두는 것도
막혀 있다 — README 에 빈 표가 나온다.

**개요 격자는 위에 몰아 두고 상세는 그 아래로 내린다.** 분류마다 격자와 상세를 번갈아
놓으면 전체를 한눈에 볼 수 없다. 둘째 분류를 보려면 첫째 분류의 상세 여섯 개를 지나쳐야 한다.

**분류가 `###` 이고 계열이 `####` 다.** 깃허브 문서 목차가 그 층으로 접어준다.
분류 이름과 계열 이름이 겹치면 앵커가 겹치므로 생성이 멈춘다.

**짧은 줄은 빈 칸으로 채워 늘 세 칸으로 만든다.** `width="33%"` 는 그 표 안에서만 33% 라,
계열이 둘뿐인 분류는 표가 두 칸짜리가 되어 그림이 분류마다 다른 자리에 놓인다.
지금은 6/3/6 이라 안 드러나지만 계열 하나만 더해도 드러난다.

한 계열의 변형은 **넷까지**다. 넷이면 README 에서 2x2 로 접힌다 —
한 줄에 넷을 놓으면 폰에서 옆으로 밀어야 하지만, 계열 안에서는 표 하나에 갇혀 있어
두 줄이어도 한 묶음으로 읽힌다. 다섯째가 필요하면 계열을 쪼갠다.

**자세한 화면은 `<details>` 로 접는다.** 채팅방 그림만 펼쳐 두고 목록·잠금화면·실행화면은
필요할 때 연다. 한 계열에 변형이 셋이면 그림이 열두 장이라 다 펼치면 스크롤을 감당 못 한다.

**표 안에 긴 문장을 넣지 않는다.** 받기 링크도 `iOS` / `Android` 처럼 짧게 쓴다.

**README 의 테마 목록도 생성물이다.** `<!-- THEMES:START -->` 와 `<!-- THEMES:END -->` 사이는
`tools/preview.py` 가 채운다. 손으로 고치지 말고 팔레트 표의 `name` 과 `note` 를 고친다.
마커 바깥의 글은 손으로 쓴다.

**README 안에서 뛰는 링크는 `#user-content-` 로 시작한다.** GitHub 은 문서 안의 id 앞에
`user-content-` 를 붙여 두고, 링크를 누르면 자바스크립트가 그 접두사를 채워 찾아간다.
그 스크립트가 안 도는 자리가 있다 — 모바일 브라우저와 GitHub 앱. 거기서는 `#먹빛-민트` 를
눌러도 아무 일도 안 난다. 처음부터 진짜 id 를 가리키면 브라우저가 알아서 찾아간다.

## 릴리스 규칙

**릴리스마다 모든 테마의 `.ktheme` 과 `.apk` 를 전부 자산으로 붙인다.** 한쪽만 올리지 않는다.
받는 사람은 저장소가 아니라 릴리스 자산에서 받는다 — 그래서 `dist/` 는 커밋하지 않는다.

```powershell
powershell -ExecutionPolicy Bypass -File release.ps1 -Version 0.4 -NotesFile notes.md
```

`release.ps1` 이 하는 일과 그 이유:

1. `tools/themes.py` 의 `VERSION` 과 요청 버전이 같은지 본다. 다르면 멈춘다.
2. 워킹 트리가 깨끗한지, 태그가 이미 있는지 본다. 중간 상태로 릴리스가 나가는 걸 막는다.
3. `build.ps1` 로 전부 빌드한다.
4. 태그를 밀고 `dist/` 의 파일을 전부 자산으로 붙인다.

**자산 이름에는 버전을 넣지 않는다** (`inkmint01.apk`). README 가
`releases/latest/download/` 로 바로 거는데, 이름에 버전이 들어가면 릴리스마다 링크가 깨진다.
버전은 태그와 릴리스 제목이 갖는다.

**품질 개선은 패치 번호로 올린다.** `0.13` → `0.13.1` → `0.13.2`.
같은 것을 더 낫게 만든 작업은 새 버전이 아니다. 마이너(`0.14`)는 테마를 더하거나
할 수 있는 것이 늘었을 때만 올린다. 이미 나간 릴리스를 덮어쓰지 않는다 —
받아 간 사람이 무엇을 받았는지 알 수 없게 된다.

`versionCode` 는 패치까지 반영한다(`mixed09` + `0.13.1` → `901301`).
자리를 넉넉히 잡아둬서 패치를 계속 올려도 번호가 내려가지 않는다.

**릴리스 노트는 직접 쓴다.** 자동 생성에 맡기지 않는다 — main 에 바로 커밋하는 프로젝트라
자동 노트는 링크 한 줄만 남고 비어버린다. 확인 못 한 것도 적는다.

## 받는 길

**iOS 링크는 Pages 의 `docs/share.html` 을 거치고, 안드로이드는 릴리스 자산을 바로
가리킨다.** 비대칭이지만 의도한 것이다 — APK 는 카톡이 아니라 시스템이 설치한다.

**브라우저마다 제일 짧은 길이 다르다.** 그래서 링크를 늘리지 않고 페이지가 고르게 했다.
README 배치 규칙상 테마마다 링크를 늘릴 수도 없다.
인앱 브라우저의 바로 설치와 사파리의 내려받기는 실물로 확인했다. 깃허브 앱은 확인 전이다.

| 어디서 | 어떻게 | 결과 |
|---|---|---|
| 카카오톡 인앱 브라우저 | `files/` 파일 주소로 넘긴다 | 카톡이 가로채 **바로 설치**. 제일 짧다 |
| 사파리 · 깃허브 앱 | 공유 시트를 띄운다 | 카톡으로 보내고 채팅방에서 눌러 설치 |
| 데스크톱 | 파일로 내려받는다 | |

핵심은 헤더 하나다. **릴리스 자산은 `Content-Disposition: attachment` 를 달고 나온다.**
그게 붙으면 브라우저는 내려받는 것 말고 아무것도 못 한다. **Pages 의 같은 파일에는 그 헤더가
없어서** 카카오톡이 가로챌 여지가 남는다. **그래서 iOS 쪽에 릴리스 자산 주소를 쓰면 안 된다** —
이게 "받는 사람은 릴리스 자산에서 받는다" 규칙의 유일한 예외다.

**사파리는 그래도 안 된다.** 그 헤더가 없어도 `.ktheme` 을 그냥 내려받는다. 카카오 공식
안내는 사파리에서도 〈카카오톡으로 열기〉 가 뜬다고 적고 있지만 지금 iOS 에서는 아니다.
문서를 믿고 직링크로 바꾸지 말 것. 확인한 결과가 위 표다.

**인앱 브라우저는 UA 의 `KAKAOTALK` 으로 가린다.** 다른 사이트의 링크를 타고 들어오면
그 표시가 빠질 수 있는데, 못 알아봐도 괜찮게 짜여 있다 — 그때는 공유 갈래로 갔다가
파일 공유가 없어서 결국 파일 주소로 떨어지고, 같은 자리로 돌아온다.

**공유 시트에서는 링크 보내기가 기본이다.** 채팅방에서 그 링크를 누르면 인앱 브라우저가
열려 첫 번째 길을 탄다. 파일 보내기보다 짧고, 내려받을 것이 없어 기다림도 없다.
파일 보내기는 방에 파일 자체를 남기고 싶을 때 쓴다.

**공유 페이지가 읽는 바이트도 사이트 안에서 온다.** 릴리스 자산에는
`Access-Control-Allow-Origin` 이 없어서 `fetch` 로 못 읽는다. `api.github.com` 입구에는
붙어 있지만 최종 목적지인 `release-assets.githubusercontent.com` 에 없고,
`fetch` 는 리다이렉트 매 홉을 검사하므로 거기서 막힌다. 그래서 Pages 워크플로가
릴리스에서 `.ktheme` 을 받아 사이트에 한 벌 둔다. **저장소에 커밋하는 게 아니다** —
`dist/` 를 커밋하지 않는다는 규칙은 그대로다.

**사이트는 저장소 구조를 그대로 옮긴다.** `docs/` 는 `docs/` 로, `assets/` 는 `assets/` 로.
그래야 `docs/index.html` 이 쓰는 `../assets/` 경로가 로컬에서 파일로 열 때와 사이트에서
똑같이 동작한다. 경로를 두 벌 관리하지 않으려는 것이다. 내려받은 테마는 `files/` 에 들어간다.

**릴리스를 내면 사이트도 따라간다.** 워크플로가 `release: published` 에 걸려 있어서
새 릴리스의 자산으로 갱신된다. 파일 이름을 바꾸는 릴리스를 낼 때는 README 링크와
사이트의 파일 이름이 같이 바뀌므로 따로 할 일이 없다.

**공유 단추가 눌린 뒤에 파일을 받아오면 안 된다.** `share()` 는 탭이 만든 일시적 활성화를
소비하는데 그 창이 짧다. 누른 뒤에 내려받기 시작하면 느린 회선에서 `NotAllowedError` 로
떨어진다. 페이지를 열 때 미리 받고, 다 받기 전까지 단추를 잠가 둔다.

## 다른 세션이 같은 저장소를 만지고 있을 때

**워킹 트리를 건드리지 않는다.** 별도 작업 폴더에서 하고, 갈아끼울 준비까지만 하고 멈춘다.

도는지 아닌지는 시작 전에 본다. 하나라도 걸리면 도는 것으로 친다.

- `git status` 에 **내가 만들지 않은 변경**이 있다
- 파이썬이나 `aapt2`·`java` 프로세스가 방금 떠 있다
- `.git/index.lock` 이 있다

같은 트리에서 같이 작업하면 두 가지가 깨진다. 저쪽의 `git add -A` 가 내 변경을
자기 커밋에 쓸어 담고, `build-src/` 는 테마마다 지워지고 다시 만들어지므로
양쪽 빌드가 서로의 중간 산출물을 읽는다.

```bash
git worktree add <임시경로> -b wip/<이름> HEAD
```

작업 폴더는 `git worktree` 로 만든다. 복사본과 달리 커밋을 남길 수 있어서
나중에 합칠 때 무엇을 바꿨는지가 그대로 남는다. **HEAD 에서 딴다** —
저쪽이 고치는 중인 파일은 반쯤 쓰여 있을 수 있다.

끝나면 거기서 커밋만 해두고 사용자에게 알린다. 합치는 것은 저쪽 작업이 끝난 뒤다.

```bash
git merge wip/<이름>        # 저쪽이 끝난 뒤
git worktree remove <임시경로>
git branch -d wip/<이름>
```

## 저장소에 넣지 않는 것

`.gitignore` 에 있지만 이유를 적어둔다. 파일은 로컬에 그대로 둔다 — 지우는 게 아니다.

- `dist/`, `build-src/`, `build-tmp/` — 생성물. 원본은 팔레트 표뿐이다.
- `theme.keystore` — 서명 키. 공개 저장소에 올라가면 누구나 같은 서명의 APK 를 만들 수 있다.
  잃어버리면 같은 패키지의 새 버전을 덮어 설치할 수 없다. 폰에서 지우고 새로 깔면 된다.

## 리소스 이름

블록·속성·리소스 이름은 **카카오 공식 테마 가이드에서만** 가져온다.
남의 테마를 뜯어서 베이스로 쓰지 않는다 — 배포 조건이 대개 재배포 금지다.

- [iOS 가이드](https://t1.kakaocdn.net/kakaocorp/Service/Theme/KakaoTalk/9.2.5_UserThemeGuide_iOS.pdf)
- [Android 가이드](https://t1.kakaocdn.net/kakaocorp/Service/Theme/KakaoTalk/9.2.5_UserThemeGuide_Android.pdf)

가이드 PDF 는 폰트 인코딩이 밀려 있어서 그냥 열면 안 읽힌다. 글자 코드에 +31 하면
영문이 나온다(`0x42`~`0x5B` → `a`~`z`, `0x40` → `_`, `0x0E` → `-`, `0x0F` → `.`,
`0x11`~`0x1A` → `0`~`9`). 한글은 커스텀 인코딩이라 복구가 안 된다. 이름만 뽑는 용도로 쓴다.

**규격 전체는 `docs/spec.md` 에 정리해뒀다.** 0.23 부터 가이드에 적힌 자리를
전부 쓴다 — 색, 말풍선, 배경 셋, 탭 아이콘, 기본 프로필, 친구추가 단추,
잠금화면 동그라미 여덟 장, 키패드 눌림, 탭바 배경.

**이름을 지어내지 않는다.** 가이드에 없는 속성은 쓰지 않는다 — iOS 탭바에
이미지를 넣는 속성이 없어서 거기만 색으로 둔 것이 그런 경우다. 카톡은 모르는
이름을 조용히 무시하므로, 지어낸 이름은 폰에서도 안 걸리고 그냥 안 먹는다.

## 함정

- **`.ktheme` 을 안드로이드에 넣을 수 없고 반대도 안 된다.** 포맷이 완전히 다르다.
- **iOS 말풍선은 색상값으로 못 바꾼다.** `MessageCellStyle` 에 `background-color` 가 없다. PNG 를 그려야 한다.
- **실행화면(스플래시)은 안드로이드만 된다** (`theme_splash_image`). iOS 규격에는 블록 자체가 없다.
- 안드로이드 테마 APK 는 권한을 하나도 요구하지 않는다. 설치할 때 권한 안내가 뜨면 잘못된 것이다.
- gradle 은 쓰지 않는다. 코드 없는 리소스 전용 APK 라 `aapt2` → `zipalign` → `apksigner` 면 끝난다.
- **네이티브 exe 의 stderr 를 `2>&1` 로 받지 않는다.** PowerShell 5.1 은 그렇게 받으면 종료코드가
  0이어도 `NativeCommandError` 로 승격시켜 빌드가 실패한 것처럼 보인다. apksigner 가 여기 걸렸다.
- **한글을 네이티브 프로그램의 명령줄 인자로 넘기지 않는다.** PowerShell 이 시스템 코드페이지로
  인코딩해서 깨진다. `gh release create --title "카카오톡 테마 0.3"` 이 "移댁뭅?ㅽ넚" 이 됐다.
  파일을 거쳐 보낸다 — `git tag -F`, `gh api --input <json>`. 표준입력이나 `--notes-file` 로
  들어가는 내용은 멀쩡하다. 인자만 문제다.
- **파이썬 출력은 UTF-8 로 고정한다.** 윈도우 기본이 cp949 라 한글이 깨진다.
  `sys.stdout.reconfigure(encoding='utf-8')` 을 도구 맨 위에 둔다.
  PowerShell 쪽은 `[Console]::OutputEncoding` 을 UTF-8 로 둔다.
- **PIL 의 `Image.effect_noise` 는 씨앗을 못 준다.** 파이썬 `random` 이 아니라 C 쪽 난수를
  쓰고 씨앗을 줄 자리가 없다. 밤하늘의 밴딩 깨는 잡음이 여기 걸려서, 별은 키에서 나온
  씨앗으로 고정됐는데 잡음만 매번 달라졌다. 심야 그림 여덟 장이 미리보기를 돌릴 때마다
  diff 에 떠 있었고, 아무도 고친 적 없는 파일이라 그게 무엇인지 한참 안 보였다.
  그림에 난수를 쓸 때는 `random.Random(씨앗)` 으로 직접 뽑는다.
  **절반 크기로 만들어 키우지 않는다** — 키울 때 생긴 두 픽셀짜리 알갱이가 흐림을 견뎌
  PNG 가 두 배가 된다(108KB → 197KB). 잡음은 목표 크기로 그대로 뽑는다.
- **RGBA 이미지에 알파를 섞어 그리면 PIL 은 섞지 않고 덮어쓴다.** `ImageDraw.Draw(img, 'RGBA')`
  는 바탕이 RGB 일 때만 알파를 섞는다. RGBA 바탕에서는 알파까지 통째로 바뀌고, 나중에 RGB 로
  바꿀 때 그 색이 원색으로 나온다. 나무결에서 알파 15 로 칠한 판자가 원색 띠로 나왔다.
  바탕을 RGB 로 두고 그리거나, 따로 만든 판에 그려 `alpha_composite` 로 얹는다.
- **RGB 로 색을 밝히면 진한 색이 회색이 된다.** 채널마다 255 쪽으로 당기는 `lighten()` 은
  어두울수록 세 채널이 나란히 올라가 채도를 잃는다. 레드의 받은 말풍선(`#3B0B16`)은
  채도 0.81 이 0.09 로 떨어져 글로우 테두리가 잿빛이었다 — 빨간 테마인데 테두리만
  회색이면 빛이 아니라 덧그린 선으로 보인다. 밝은 파스텔은 원래 채도가 낮아 티가 안 나서,
  어떤 계열은 예쁘고 어떤 계열은 아니었다. 빛을 그릴 때는 `glow_tint()` 처럼 색상을 두고
  명도만 올린다. 순백을 진하게 얹는 것도 같은 문제다 — 아래 깔린 빛을 덮어 흰 선이 된다.
- **안드로이드 `versionCode` 를 목록 순서로 매기지 않는다.** 테마 순서를 바꾸면 번호가
  내려가는 테마가 생기고, 안드로이드는 그걸 다운그레이드로 보고 설치를 거부한다.
  키 끝의 번호와 테마 버전으로 만든다 — `mixed09` + `0.8` → `9008`.
- PowerShell 스크립트는 **UTF-8 BOM** 으로 저장한다. BOM 이 없으면 5.1 이 ANSI 로 읽어 한글이 깨진다.
- **릴리스 자산은 자바스크립트로 못 읽는다.** CORS 헤더가 없다. 브라우저에서 파일 내용이
  필요하면 Pages 사이트 안(같은 출처)이나 `raw.githubusercontent.com` 에서 읽어야 한다.
- **릴리스 자산 링크로는 `.ktheme` 을 카톡에 바로 못 넘긴다.** `Content-Disposition: attachment`
  가 붙어 나와서 브라우저가 내려받기만 한다. Pages 의 같은 파일에는 그 헤더가 없다.
- **안드로이드는 `.apk` 를 웹 공유로 못 보낸다.** 크롬의 공유 API 는 확장자 허용목록 방식이라
  실행 파일류가 아예 빠져 있다. 공유 방식은 iOS 전용으로 생각한다.
