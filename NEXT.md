# NEXT

- 여기까지 됨: 네온사인 v6~v13 과 네온 동물, 아홉 계열 36벌을 더했다(`wip/neon-v6`, 292벌 73계열, 0.38). 벽을 걷어내고 빛으로 채운 배경을 쓴다(`scenes.neonbg` — 별자리·빛 꽃잎·보케·플라즈마·잉크·레이저 그물·크리스탈·색수차). `neonwall` 은 안 건드려서 v1~v5 그림은 바이트까지 같다(전체를 다시 그려 확인했고 바뀐 assets 이 0개다). 말풍선 네 가지는 v4·v5 와 같고 배경과 색 조합만 바꾼다 — 색은 시안 열두 벌에서 계열마다 한 벌씩 골랐다. 동물 계열만 축이 달라서 말풍선을 두 가닥으로 고정하고 네 벌을 식구(바다·숲·얼음·모둠)로 갈랐고, 프로필 세 장도 그 식구다(`charbubble.p_neonzoo`). 동물 좌표는 `tools/zoo.py` 에 따로 뒀다 — scenes 와 charbubble 이 서로를 안 부르게 하려는 것이다. 카카오 프렌즈와 겹치는 여덟(사자·복숭아·토끼·개·고양이·오리·두더지·악어)은 피했다. README 대표 셋을 `네온사인 · v7 · 네온 동물` 로 바꿨다. 0.37 에는 README 를 카테고리마다 대표 셋으로 줄이고 네온사인을 `lights-neon.md` 로 뺐다.
- 다음 할 것: `wip/neon-v6` 을 main 에 합친다 — 뽑을 때 다른 세션이 `tools/themes.py` 를 고치는 중이었고 java 빌드가 돌고 있어서 합치지 않고 멈췄다. 그쪽이 끝났는지 보고 합친다(`tools/themes.py` 는 충돌이 날 수 있다). 합친 뒤 0.38 을 릴리스하고, 릴리스 노트에 `불빛 - 네온사인` 대표 셋이 v4·v5 에서 v7·네온 동물로 바뀐 것을 적는다. 새 36벌을 폰에서 본다. 저장소에 LICENSE 가 없다 — public 인데 표기가 없으면 모든 권리 보유라서 Apache-2.0 을 넣을지 정한다.
- 막힌 것: 테마 규격에 글꼴 자리가 없어 말풍선 글자는 늘 기본 글꼴이다. worktree 에는 `theme.keystore` 가 없어서 거기서 빌드하면 새 키가 만들어진다 — 릴리스는 원래 폴더에서 하거나 키를 복사한다. 글자 단위로 합친 README·카테고리 문서는 칸이 밀릴 수 있어 합친 뒤 미리보기를 다시 그린다. 사파리 한 번에 설치는 여전히 길이 없다.
