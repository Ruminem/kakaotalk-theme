# NEXT

- 여기까지 됨: 0.35 를 냈다. 유리 카테고리에 씨글래스·글래스 블록·얼음·골판 유리·아크릴·레진 여섯 계열(24벌)을 더했다. 기존 유리가 둥근 네모에 테두리 색만 달라서, 말풍선 표면(charbubble)과 뒤 그림(scenes)으로 재질을 드러내고 `bubble_style='glass'` 로 유리구슬 프로필과 반투명 셀을 받는다. 콘셉트 시안 일곱을 그려 여섯을 골랐다(크리스탈 컷은 말풍선이 보라 네모로 읽혀 뺐다). 목록 배경은 흐리지 않고 큰 조각만 뺐다(`bare`) — 흐렸더니 목록이 전부 단색이었다. README·카테고리 문서의 계열 아이콘을 링크 안에 넣어 누르면 PNG 화면으로 가지 않게 했다. 기존 196벌은 0.34 와 해시가 같다.
- 다음 할 것: 유리 24벌·무늬 20벌·편안함 16벌·도트 16벌을 폰에서 보기 — 반투명 말풍선이 배경 위에서 읽히는지, 첫 말 소품(나사·기포·잎·리본·낙관 등)이 긴 말에서 멀쩡한지. 실행화면 디자인은 사용자가 보류했다.
- 막힌 것: 테마 규격에 글꼴 자리가 없어 말풍선 글자는 늘 기본 글꼴이다. 반투명 점을 RGBA 판에 바로 찍으면 PIL 이 덮어써 원색이 된다 — RGB 판에 'RGBA' 로 찍는다. 파이썬으로 파일을 텍스트 모드로 다시 쓰면 CRLF 가 된다(커밋 때 LF 로 돌아가 해는 없다). 릴리스가 도는 중에는 시작 검사가 끝난 뒤에만 워킹 트리를 건드린다. 사파리 한 번에 설치는 여전히 길이 없다.
