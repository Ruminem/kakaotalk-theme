# NEXT

- 여기까지 됨: 0.32 를 냈다. 카테고리를 일곱으로 늘렸다. 차분·고요를 무늬에서 편안함으로 옮기고, 도트 카테고리를 새로 만들어 오락실을 옮기고 도트 모험·도트 액정·레트로 PC·도트 농장 네 계열(16벌)을 더했다. 도트는 카테고리 규칙(`themes.CATEGORY_RULES`)으로 한 칸 4pt 말풍선·아래 한 칸 그림자·12칸 도트 탭 아이콘·흐리지 않는 목록 질감·배경 한 칸 3% 를 모든 계열에 붙이고, 오락실은 이미 나간 테마라 예외로 뺐다. 처음엔 한 칸 2pt 로 그렸다가 사용자가 "8비트 감성이 아니다" 고 해서 키웠다. 저장소 글의 "분류" 를 전부 "카테고리" 로 바꿨다(계열은 그대로). 0.31.3 생성물과 해시로 대조해 기존 144벌은 한 파일도 안 바뀌었다.
- 다음 할 것: 도트 16벌을 폰에서 보기(첫 말 소품이 늘어나는 말풍선에서 멀쩡한지, 도트 농장 새싹이 작지 않은지). 실행화면 디자인은 사용자가 보류했다.
- 막힌 것: 테마 규격에 글꼴 자리가 없어 도트 말풍선 안 글자는 매끈한 기본 글꼴이다. Bash heredoc 으로 긴 파이썬 코드를 붙이면 따옴표 해석에서 깨질 때가 있다 — 파일로 쓰고 붙인다. 그림 뷰어에 열린 PNG 는 덮어쓰기가 막히니 새 이름으로 저장한다. 방향 있는 빛(캠핑 불빛)은 번질 거리가 가로세로 합쳐 35pt 쯤이 한계다. 윈도우에서 멀티프로세싱 스크립트를 표준입력으로 넘기면 자식 프로세스가 끝없이 다시 뜬다 — 파일로 저장해 돌린다. 사파리 한 번에 설치는 여전히 길이 없다.
