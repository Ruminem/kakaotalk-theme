# NEXT

- 여기까지 됨: README 를 카테고리마다 대표 셋 + `전부 보기` 링크로 줄이고(`CATEGORY_COVER`) 받는 법을 테마 위로 올렸다. 네온사인 다섯 계열을 `불빛 - 네온사인`(`lights-neon.md`)으로 뺐다 — 다음 릴리스 노트에 `lights.md` 네온사인 링크가 옮겨졌다고 적는다. `wip/solid-lights` 는 이미 나간 블루·퍼플·그린·핑크의 키를 내리고 골드를 빼서 지웠다. 임시 링크 테스트 절과 `docs/anchor-test.md` 를 지웠다. 0.37 을 냈다. 네온사인 v4 판자 벽을 wood() 의 나이테 결과 가운데 천장 조명으로 다시 그렸다(곧은 선 결이 바코드로 보였다). 네온사인 v5(키 313~316, neonlounge)를 더했다 — v4 말풍선 네 가지를 짙은 차콜 미장 벽에 걸고 위쪽 간접등 둘, 낙서 셋. 잎 벽·대리석·벨벳·가죽은 요란하거나 품질이 안 나와 버렸다. 0.36 에는 레드 파생 다섯 계열과 네온사인 v2·v3 이 나갔다.
- 여기까지 됨(2026-09-18): 커스텀 테마 섞기를 더했다. `tools/mix.py` 가 배경 테마의 그림 셋(chat/main/passcode/flat_list)과 말풍선 테마의 말풍선·색을 통째로 합쳐 dict 하나를 만들고 `gen.build_one` 에 넘긴다. 포인트색만 따로 덮어쓸 수 있다. `themes.file_slug` 는 dict 에 `slug` 가 있으면 그걸 먼저 쓴다(커스텀은 계열도 변형도 없어 VARIANT_SLUG 에서 이름 조각을 못 찾는다). 기존 256벌에는 `slug` 키가 없어 영향이 없다. `.github/workflows/custom.yml` 이 windows 러너에서 같은 걸 돌려 `.ktheme`·`.apk`·`preview.png` 를 artifact 로 올린다. README 에 `섞어 만들기` 절을 넣었다.
- 다음 할 것: **`커스텀 테마 만들기` 워크플로를 한 번 실제로 돌려 본다 — 아직 CI 에서 돌린 적이 없다.** 러너에 `ANDROID_HOME` 과 `keytool` 이 있다고 보고 짰다. 그 다음은 v4·v5 와 0.36 의 새 32벌을 폰에서 보기. 합친 뒤 릴리스 전까지 README 의 새 테마 받기 링크가 404 라서, 새 계열은 합치고 바로 릴리스하거나 아직 안 나간 테마를 "준비 중" 으로 표시할지 정한다. 실행화면 디자인은 사용자가 보류했다.
- 막힌 것: 테마 규격에 글꼴 자리가 없어 말풍선 글자는 늘 기본 글꼴이다. worktree 에는 `theme.keystore` 가 없어서 거기서 빌드하면 새 키가 만들어진다 — 릴리스는 원래 폴더에서 하거나 키를 복사한다. 글자 단위로 합친 README·카테고리 문서는 칸이 밀릴 수 있어 합친 뒤 미리보기를 다시 그린다. 사파리 한 번에 설치는 여전히 길이 없다.
