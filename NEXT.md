# NEXT

- 여기까지 됨: `불빛 - 단색` 분류(`docs/themes/lights-solid.md`)를 만들고 레드를 불빛에서 옮겼다. 레드의 색을 색상만 돌린 단색 계열 열 개(오렌지·옐로·라임·그린·민트·시안·블루·바이올렛·퍼플·핑크)를 `solid_light()` 로 네 벌씩, 모두 마흔 벌 더했다. 키는 orange200~pink239. 전체 생성과 미리보기를 돌려 기존 그림은 한 장도 안 바뀐 것을 확인했다. `wip/solid-lights` 가지의 작업 폴더에서 했다 — 다른 세션이 main 워킹 트리에서 편안함·픽셀 분류를 만드는 중이었다.
- 다음 할 것: 저쪽 작업이 끝나면 `git merge wip/solid-lights` — themes.py 의 CATEGORIES·CATEGORY_FILE·CATEGORY, README 소개 줄에서 겹칠 것이다. 합친 뒤 `python tools/gen.py` 로 README·분류 문서·docs/index.html 을 다시 만든다. 테마를 더했으니 릴리스는 마이너(0.32)로 올린다. 폰에서 새 마흔 벌 확인.
- 막힌 것: 옐로·라임·그린·민트·시안의 받은 말풍선 글자 대비가 3.7~4.4 로 4.5 에 조금 못 미친다 — 레드를 따라 짙은 칸과 밝은 칸이 섞여 한 글자색으로 둘 다 맞출 수 없다. 원본 레드의 보낸 말풍선(2.7)보다는 낫다. 옐로의 밝은 받은 칸(#8E730F)은 노랑이 어두워지며 겨자색으로 보인다.
