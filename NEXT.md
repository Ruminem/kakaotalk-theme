# NEXT

- 여기까지 됨: 0.31.3 을 냈다. 0.31.2 에서 네온사인에 넣은 목록 화면 규칙을 모든 계열로 넓혔다 — 캐릭터가 없는 21계열은 기본 프로필을 계열 물건 세 가지로(`tools/motif.py` 의 `MOTIFS`, 글로우+어두움은 네온관), 유리 다섯 계열은 배경을 거꾸로 비추는 유리구슬로, 글로우 변형은 탭 아이콘을 네온관으로, 목록 배경 그림이 있는 변형은 셀을 0.72 로 반투명하게 했다. 모티프를 안 적은 새 계열은 생성이 멈춘다. 0.31.2 생성물과 해시로 대조해 바뀐 것은 프로필 104벌·탭 아이콘 42벌·셀 투명도 54벌뿐이고 말풍선·채팅방·잠금화면 그림은 그대로다. 유리 프로필은 처음에 반투명 판 위에 반짝이를 올렸다가 사용자가 "짜친다" 고 해서 구슬로 바꿨다.
- 다음 할 것: 폰에서 목록 화면 보기 — 셀 반투명(0.72, 네온사인 0.5)에서 글자가 잘 읽히는지, 36pt 모티프가 알아볼 만한지(연등이 조금 통처럼 보인다). 밝은 글로우 변형의 네온 탭은 대비가 낮을 수 있다. 0.30~0.31 새 테마 마흔네 개 폰 확인도 남았다.
- 막힌 것: 방향 있는 빛(캠핑 불빛)은 번질 거리가 가로세로 합쳐 35pt 쯤이 한계다. 윈도우에서 멀티프로세싱 스크립트를 표준입력(`python -`)으로 넘기면 자식 프로세스가 끝없이 다시 떠서 멈춘 것처럼 보인다 — 파일로 저장해 돌린다. README 와 분류 문서만 다시 쓰고 전체 생성을 안 돌리면 미리보기 함수의 이름 겹침 같은 오류를 못 잡는다 — preview.py 를 고치면 적어도 배경 있는 테마 하나는 그려 본다. 사파리 한 번에 설치는 여전히 길이 없다.
