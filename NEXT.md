# NEXT

- 여기까지 됨: 폰에서 테마 받는 길이 링크 한 번으로 줄었다. GitHub Pages 를 켜고(Actions 배포) 릴리스의 `.ktheme` 을 사이트 `files/` 에 두게 했더니, 카카오톡이 그 링크를 눌렀을 때 파일을 그대로 가로채 바로 설치한다 — `테마 다운로드 완료` 가 뜨고 끝이다. 채팅방에 보내고 파일을 누르고 적용하는 세 단계가 통째로 없어졌다. 차이는 `Content-Disposition: attachment` 하나였다. 릴리스 자산에는 그게 붙어 나와서 브라우저가 내려받기밖에 못 했다. README 의 iOS 링크 52개가 Pages 를 가리키고, 안드로이드는 릴리스 자산 그대로다. `docs/share.html`(공유 시트로 넘기는 페이지)은 두 번째 길로 남겼다.
- 다음 할 것: **사파리와 깃허브 앱에서도 같은지 확인.** 카카오톡 인앱 브라우저에서만 실물로 봤다. 카카오 공식 안내는 사파리도 된다고 하지만(〈카카오톡으로 열기〉) 확인 전이다. 그다음은 하단 탭 아이콘 8종 — 양쪽 다 아직 카톡 기본값이다.
- 막힌 것: 사파리가 `application/octet-stream` 을 그냥 내려받아 버리면 iOS 링크를 `docs/share.html?f=<이름>.ktheme` 로 되돌린다 — `tools/preview.py` 의 `IOS` 상수 한 줄이다. Pages 와 워크플로는 어느 쪽이든 그대로 쓴다. 릴리스 직후 Pages 배포 전 몇 분 동안은 iOS 링크가 404 다(워크플로가 `release: published` 에 걸려 있다). 안드로이드는 `.apk` 를 웹 공유로 못 보내니(크롬이 확장자 허용목록) 계속 릴리스 직링크다.
