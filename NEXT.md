# NEXT

- 여기까지 됨: 0.31.1 을 냈다. 터미널 글로우 두 벌의 흰 테두리·반사선을 걷고 모니터 창틀처럼 빛나게 했다(`glow_edge='phosphor'`) — 다른 142개 테마는 해시로 같다. 같은 흰 테두리를 쓰는 옛 글로우 테마가 37벌 더 있어서, 거슬리는 계열이 있으면 그 계열에만 `glow_edge` 를 적으면 된다. 그 전 0.31 에서 불빛 분류에 네온사인 네 벌(이중관·간판·전극·말꼬리)을 더해 테마가 144개다. 글로우를 `tools/glow.py` 사전으로 모아 이름으로 고르게 했고, 재질(`material`)을 적으면 어울리는 모양이 자동으로 붙는다 — 네온관은 `bloom`, 간판은 `drop`. 기존 글로우는 `char` 로 그대로 옮겨 140개 테마 그림이 해시로 같다. `tools/glowsheet.py <계열>` 로 모든 글로우를 한 장에 비교한다. 릴리스 노트에 계열마다 채팅방 미리보기를 2x2 격자로 접어 붙였고(0.30·0.31), 그림 주소는 태그에 고정했다. 다른 세션이 README 를 분류 문서(`docs/themes/*.md`)로 나눈 것과 합쳤다.
- 다음 할 것: 0.30~0.31 의 새 테마 마흔네 개를 폰에 깔아 보기 — 캠핑 어두움+배경의 불빛, 빵집 잼, 네온사인 전극의 끊긴 틈처럼 미리보기에서 작거나 은은한 것들. 네온사인 기본 프로필 캐릭터 그리기(지금은 기본 사람 모양). 앞으로 릴리스 노트의 미리보기 격자는 `release_gallery` 방식을 도구로 저장소에 넣을지 정하기. 그다음은 깃허브 앱 웹뷰의 받는 길과 WebP 표시 확인, 하단 탭 아이콘 8종.
- 막힌 것: 방향 있는 빛(캠핑 불빛)은 번질 거리가 가로세로 합쳐 35pt 쯤이 한계다 — cap 이 가로세로 한 값이라 한 줄짜리 말풍선에서 늘어나는 줄이 지날 틈을 남겨야 한다. 윈도우에서 멀티프로세싱 스크립트를 표준입력(`python -`)으로 넘기면 자식 프로세스가 스크립트를 못 읽고 끝없이 다시 떠서 멈춘 것처럼 보인다 — 파일로 저장해 돌린다. 사파리 한 번에 설치는 여전히 길이 없다.
