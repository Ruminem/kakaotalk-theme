# NEXT

- 여기까지 됨: 폰에서 받는 길을 고쳤다. GitHub Pages 를 켜고(Actions 배포) `docs/share.html` 을 냈다 — README 의 iOS 링크가 거기로 가고, 버튼 하나가 `.ktheme` 을 iOS 공유 시트에 바로 올린다. 다운로드 받아 파일 앱에서 찾는 구간이 없어졌다. 릴리스 자산에는 CORS 헤더가 없어 fetch 로 못 읽으므로, 워크플로가 릴리스에서 `.ktheme` 을 받아 사이트 `files/` 에 한 벌 둔다(저장소에는 안 넣는다). 사이트는 살아 있고 파일·그림·페이지 전부 200 으로 응답한다.
- 다음 할 것: 아이폰에서 실제로 눌러보기. 공유 시트에 카카오톡이 뜨는지, 파일 이름이 그대로 넘어가는지, 채팅방에서 눌렀을 때 설치되는지 셋을 확인한다. 그다음은 하단 탭 아이콘 8종 — 양쪽 다 아직 카톡 기본값이다.
- 막힌 것: 공유 시트가 `.ktheme` 을 받아 카톡에 넘기는지를 실물로 확인 못 했다. WebKit 소스에는 MIME 허용목록이 없고 파일 이름을 확장자째 보존해 `UIActivityViewController` 로 넘기므로 될 것으로 보지만, 확인 전이다. 안 되면 share.html 만 되돌리면 되고 워크플로와 사이트는 그대로 쓸 수 있다. 안드로이드는 `.apk` 를 웹 공유로 못 보내니(크롬이 확장자 허용목록) 계속 릴리스 직링크다.
