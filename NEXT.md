# NEXT

- 여기까지 됨: 0.31.2 를 냈다. 네온사인 목록 화면을 네온으로 채웠다 — 기본 프로필 세 장을 하트·별·초승달 네온관으로(`charbubble.PROFILES`), 탭 아이콘 7종을 같은 도형의 네온관으로(`gen.tab_icon_for`, `tab_style='neon'`, 선택된 탭만 불이 켜짐), 목록 배경은 낙서를 뺀 벽돌 질감을 흐리지 않고 깔고 셀을 반투명(0.5)하게 했다. 네온사인 말고 140개 테마는 말풍선·CSS·프로필·탭 아이콘·목록 배경 해시가 같다. README·분류 문서 격자의 대표 사진은 배경 그림이 있는 변형으로 바꿨다(`preview.grid_cover`). 지난 릴리스 29개의 노트 맨 위에도 새로 생기거나 바뀐 테마의 미리보기를 태그 고정 주소로 접어 붙였다(0.3~0.31, 모두 520장). 그 전 0.31.1 은 다른 세션이 터미널 글로우의 흰 테두리를 모니터 창틀 빛으로 바꿨다(`glow_edge='phosphor'`).
- 다음 할 것: 네온사인 목록을 폰에서 보기 — 셀 반투명 0.5 에서 글자가 잘 읽히는지. 결과가 괜찮으면 같은 방식(계열마다 그린 프로필, 반투명 셀과 잘려도 되는 질감 배경, 계열 모양 탭 아이콘)을 다른 계열로 넓힐지 사용자와 정하기 — 대부분의 계열 목록이 아직 색만 바꾼 판이다. 0.30~0.31 새 테마 마흔네 개 폰 확인도 남았다.
- 막힌 것: 방향 있는 빛(캠핑 불빛)은 번질 거리가 가로세로 합쳐 35pt 쯤이 한계다. 윈도우에서 멀티프로세싱 스크립트를 표준입력(`python -`)으로 넘기면 자식 프로세스가 끝없이 다시 떠서 멈춘 것처럼 보인다 — 파일로 저장해 돌린다. README 와 분류 문서만 다시 쓰고 전체 생성을 안 돌리면 미리보기 함수의 이름 겹침 같은 오류를 못 잡는다(`cover` 가 그렇게 들어갔다) — preview.py 를 고치면 적어도 배경 있는 테마 하나는 그려 본다. 사파리 한 번에 설치는 여전히 길이 없다.
