# NEXT

- 여기까지 됨: 0.34 를 냈다. 무늬 카테고리에 깅엄 체크·폴카 도트·테라조·마린 스트라이프·체커보드 다섯 계열(20벌)을 더했다. 반복 무늬 배경(목록에서도 흐리지 않음), 단색 말풍선에 첫 말 소품 하나, 계열마다 따로 그린 프로필 물건과 판에 깐 무늬 타일(`motif_tile`). 콘셉트 시안을 크림 라떼를 복사해 그렸다가 프로필이 전부 커피잔으로 나와 사용자가 지적했다 — 시안에서도 계열 이름과 모티프를 새로 준다. 기존 176벌은 0.33 과 해시가 같다.
- 다음 할 것: 무늬 20벌·편안함 16벌·도트 16벌을 폰에서 보기 — 첫 말 소품(리본·물방울·돌 조각·깃발·낙관·접힌 귀퉁이)이 긴 말에서 멀쩡한지. 실행화면 디자인은 사용자가 보류했다.
- 막힌 것: 테마 규격에 글꼴 자리가 없어 말풍선 글자는 늘 기본 글꼴이다. 파이썬으로 파일을 텍스트 모드로 다시 쓰면 윈도우에서 CRLF 가 된다(커밋 때 LF 로 돌아가 해는 없다) — `newline=''` 을 준다. Bash heredoc 으로 긴 파이썬 코드를 붙이면 깨질 때가 있다 — 파일로 쓰고 붙인다. 그림 뷰어에 열린 PNG 는 덮어쓰기가 막힌다. 사파리 한 번에 설치는 여전히 길이 없다.
