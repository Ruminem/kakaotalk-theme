# NEXT

- 여기까지 됨: 폰에서 테마 받는 길을 줄였다. GitHub Pages 를 켜고(Actions 배포) 릴리스의 `.ktheme` 을 사이트 `files/` 에 두게 했다. 릴리스 자산은 `Content-Disposition: attachment` 를 달고 나와 브라우저가 내려받기밖에 못 하는데, Pages 의 같은 파일에는 그 헤더가 없어서 **카카오톡 인앱 브라우저가 파일을 가로채 바로 설치한다** — `테마 다운로드 완료` 가 뜨고 끝이다. 사파리는 헤더가 없어도 그냥 내려받아서(실물 확인) 한 주소로는 어디서나 최선이 안 된다. 그래서 `docs/share.html` 이 UA 를 보고 고른다: 카톡 인앱이면 파일 주소로 곧장, 사파리면 공유 시트(링크 보내기 기본), 데스크톱이면 다운로드. README 의 iOS 링크 52개가 그 페이지를 거치고 안드로이드는 릴리스 자산 그대로다.
- 다음 할 것: 깃허브 앱 웹뷰에서 어떻게 되는지 확인(사파리 갈래로 갈 것으로 보지만 확인 전). 그다음은 하단 탭 아이콘 8종 — 양쪽 다 아직 카톡 기본값이다.
- 막힌 것: 사파리에서 한 번에 설치되게 하는 길은 못 찾았다. 카카오 공식 안내는 사파리에서도 〈카카오톡으로 열기〉 가 뜬다고 하지만 지금 iOS 에서는 아니고, `kakaotalk://` 로 인앱 브라우저를 여는 공개 스킴도 없다(앱키 기반 스킴뿐). 릴리스 직후 Pages 배포 전 몇 분 동안은 iOS 링크가 404 다 — `share.html` 이 그때만 릴리스 자산으로 넘긴다. 안드로이드는 `.apk` 를 웹 공유로 못 보낸다(크롬이 확장자 허용목록).
