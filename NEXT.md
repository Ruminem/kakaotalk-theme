# NEXT

- 여기까지 됨: README 를 카테고리마다 대표 셋 + `전부 보기` 링크로 줄이고(`CATEGORY_COVER`) 받는 법을 테마 위로 올렸다. 네온사인 다섯 계열을 `불빛 - 네온사인`(`lights-neon.md`)으로 뺐다 — 다음 릴리스 노트에 `lights.md` 네온사인 링크가 옮겨졌다고 적는다. `wip/solid-lights` 는 이미 나간 블루·퍼플·그린·핑크의 키를 내리고 골드를 빼서 지웠다. 임시 링크 테스트 절과 `docs/anchor-test.md` 를 지웠다. 0.37 을 냈다. 네온사인 v4 판자 벽을 wood() 의 나이테 결과 가운데 천장 조명으로 다시 그렸다(곧은 선 결이 바코드로 보였다). 네온사인 v5(키 313~316, neonlounge)를 더했다 — v4 말풍선 네 가지를 짙은 차콜 미장 벽에 걸고 위쪽 간접등 둘, 낙서 셋. 잎 벽·대리석·벨벳·가죽은 요란하거나 품질이 안 나와 버렸다. 0.36 에는 레드 파생 다섯 계열과 네온사인 v2·v3 이 나갔다.
- 여기까지 됨(2026-09-18): 커스텀 테마 섞기를 더했다. `tools/mix.py` 가 배경 테마의 그림 셋(chat/main/passcode/flat_list)과 말풍선 테마의 말풍선·색을 통째로 합쳐 dict 하나를 만들고 `gen.build_one` 에 넘긴다. 포인트색만 따로 덮어쓸 수 있다. `themes.file_slug` 는 dict 에 `slug` 가 있으면 그걸 먼저 쓴다(커스텀은 계열도 변형도 없어 VARIANT_SLUG 에서 이름 조각을 못 찾는다). 기존 256벌에는 `slug` 키가 없어 영향이 없다. `.github/workflows/custom.yml` 이 windows 러너에서 같은 걸 돌려 `.ktheme`·`.apk`·`preview.png` 를 artifact 로 올린다. README 에 `커스텀 테마 제작` 절을 넣었다. 그 위에 폰에서 바로 만드는 `docs/make.html` 을 얹었다 — 사이트의 `files/` 에 `.ktheme` 가 전부 있으니, 브라우저가 두 벌을 받아 배경 여섯 장만 갈아끼우고 CSS 두 줄(테마 이름·ID)만 고친다. zip 은 라이브러리 없이 손으로 읽고 쓰고(`DecompressionStream`), 그림은 압축된 채로 옮긴다. 미리보기는 그 `.ktheme` 의 CSS 에서 cap·edgeinsets·색을 읽어 카톡처럼 한 줄만 늘여 그린다. 고를 수 있는 목록은 `preview.write_mix_manifest` 가 `docs/themes.json` 으로 낸다(배경 있는 134벌, 말풍선 쪽은 firelight 둘 제외). `node tools/check-make.mjs` 가 페이지 코드를 잘라 와 실제 테마로 섞어 보고 zip 을 검사한다 — 통과했고 파이썬 zipfile 로도 열린다.
- 여기까지 됨(2026-09-18): 네온사인 v6~v13 과 네온 동물, 아홉 계열 36벌을 더했다(292벌 73계열, 0.38). 벽을 걷어내고 빛으로 채운 배경을 쓴다(`scenes.neonbg` — 별자리·빛 꽃잎·보케·플라즈마·잉크·레이저 그물·크리스탈·색수차). `neonwall` 은 안 건드려서 v1~v5 그림은 바이트까지 같다(전체를 다시 그려 확인했고 바뀐 assets 이 0개다). 말풍선 네 가지는 v4·v5 와 같고 배경과 색 조합만 바꾼다 — 색은 시안 열두 벌에서 계열마다 한 벌씩 골랐다. 동물 계열만 축이 달라서 말풍선을 두 가닥으로 고정하고 네 벌을 식구(바다·숲·얼음·모둠)로 갈랐고, 프로필 세 장도 그 식구다(`charbubble.p_neonzoo`). 동물 좌표는 `tools/zoo.py` 에 따로 뒀다 — scenes 와 charbubble 이 서로를 안 부르게 하려는 것이다. 카카오 프렌즈와 겹치는 여덟(사자·복숭아·토끼·개·고양이·오리·두더지·악어)은 피했다. README 대표 셋을 `네온사인 · v7 · 네온 동물` 로 바꿨다. Apache-2.0 `LICENSE` 를 넣었다 — 전문은 apache.org 평문을 그대로 받았고 부록의 저작권 자리만 채웠다. 빌드에 쓰는 Pillow·qrcode 는 `.ktheme`·`.apk` 에 안 들어가서 고지할 것이 없다. **0.38 을 냈다**(자산 584개, 빌드 12분). Pages 배포와 새 `.ktheme`·`.apk` 링크, 사이트의 섞기 목록 168벌까지 눌러 확인했다. 릴리스 로그를 저장소 안(`rel.log`)에 받았다가 `release.ps1` 이 시작하자마자 커밋 안 된 변경으로 멈춰서, 로그도 표식처럼 `mktemp` 로 받으라고 CLAUDE.md 에 적었다.
- 여기까지 됨(2026-09-18 뒤): 커스텀 테마 제작에 말풍선 색 축을 넣었다. 브라우저는 말풍선을 다시 못 그려서 이미 그려둔 그림의 색상만 돌린다(채도는 비율, 명도·알파는 그대로). 다시 그린 것과 대조해 보니 채널 차이가 9/255 라 눈으로 구분이 안 된다. 되는 계열은 `preview._recolorable` 이 재서 `themes.json` 에 색을 적는다 — 지금 네온 36벌이다. 말풍선만 바꾸지 않고 배경 여섯 장 말고 모든 그림과 CSS 색을 같이 돌린다(프로필·탭 아이콘·잠금화면 점). 가르는 기준은 채도가 아니라 채도×명도다 — 네온의 검은 벽 `#100C14` 가 채도 0.40 이라 채도로 가르면 벽까지 돈다. `tools/mix.py` 는 `--send`/`--recv` 로 제대로 다시 그리고(네온 아닌 계열도 됨), 딸려 도는 색은 페이지와 같은 계산을 쓴다. `node tools/check-make.mjs` 가 두 구현을 색 일곱 개로 대조한다.
- 안드로이드는 브라우저에서 못 만든다고 결론 냈다. 테마가 설치되는 앱이라 서명이 있어야 하는데, (1) 비밀 열쇠를 페이지에 넣으면 공개되고 (2) `targetSdkVersion 34` 라 v2 서명이 필요한데 그건 파일 전체를 1MB 씩 해시해 APK Signing Block 을 끼우는 것이라 파일 구조를 다시 짜야 하고(나간 APK 를 열어 v2 블록 확인) (3) 패키지 이름이 압축된 이진 `AndroidManifest.xml`·`resources.arsc` 안에 있다. **→ 이 결론은 틀렸다. 아래 「브라우저에서 안드로이드 APK 를 만들 수 있다」 줄을 본다.**
- 안드로이드 쪽을 이슈 폼으로 열었다. `workflow_dispatch` 는 저장소 쓰기 권한이 있어야 눌려서, 남이 쓰려면 포크하고 포크에서 Actions 를 켜야 했다 — 거기서 이미 비개발자는 못 간다. 이슈 폼은 칸의 `id` 를 쿼리 파라미터로 받아 미리 채울 수 있어서(공식 문서 확인), 페이지가 고른 값을 실어 폼을 열어 주면 누를 것은 `Create` 하나만 남는다. `custom.yml` 이 `issues: opened` + `custom-theme` 딱지에도 걸리고, 끝나면 받는 곳을 댓글로 달고 이슈를 닫는다. 이슈 본문은 남이 쓴 것이라 `tools/custominput.py` 가 형식을 검사해 GITHUB_ENV 로 내보내고 뒤 단계는 그것만 본다(본문이 셸에 닿는 길을 안 만든다). 본문에는 칸의 `id` 가 아니라 `label` 이 찍혀서 `--check` 가 폼과 `LABELS` 를 대조한다. 한글을 파이썬에 인자로 넘기는 것은 멀쩡한 것을 확인했다(CLAUDE.md 의 경고는 `gh` 쪽이다).
- 테마 이름을 키워드 둘로 짓게 고쳤다. 테마 이름 둘을 이어 붙이면(`빵집 밝음+배경 · 캠핑 어두움+배경`) 이름이 아니라 고른 것의 목록이다. 양쪽에서 가져오는 것이 달라서 — 배경 쪽은 그림, 말풍선 쪽은 모양 — 각각에서 그것을 가리키는 말을 뽑는다. `캠핑 어두움 식빵`, `야경 봉투`, `설원 리본`, `네온사인 v6 이중관 라임×보라`. 말풍선 모양의 한국어 이름은 `charbubble.STYLE_WORD` 36개이고, 빠지면 목록을 낼 때 멈춘다(확인함). 제일 긴 조합이 33자에서 23자로 줄었다.
- 브라우저에서 안드로이드 APK 를 만들 수 있다는 것을 확인했다. 처음에 적어둔 "못 만든다" 의 전제 둘이 틀렸다 — 커스텀용 열쇠는 비밀일 필요가 없고(Actions 도 이미 매번 새로 만든다), 패키지 이름은 `com.kakao.talk.theme.custom` 으로 고정하면 이진 파일을 건드릴 일이 없다. 진짜 막는 것은 v2 서명 하나였고 `docs/apksign.js` 가 WebCrypto 만으로 붙인다(엔트리·중앙 디렉터리·EOCD 를 1MB 씩 해시, 서명 블록을 중앙 디렉터리 앞에 끼움, 엔트리는 안 움직여 `resources.arsc` 4바이트 정렬 유지). `node tools/check-apksign.mjs` 가 apksigner 로 검증하고 **안드로이드 14 에뮬레이터가 실제로 설치했다**. 안드로이드 폰이 없어도 `spamtest` AVD 로 판정한다 — CLAUDE.md 에 적었다. `minSdkVersion` 은 24 로 올려야 한다(v2 만 붙이므로).
- 안드로이드 커스텀 테마를 브라우저에서 만드는 길을 다 짰다. `docs/zip.js`(아이폰·안드로이드가 같이 쓰는 zip 읽기·쓰기, `align` 은 안드로이드 11 의 `resources.arsc` 4바이트 정렬용), `docs/apksign.js`(WebCrypto v2 서명), `docs/apkmix.js`(템플릿에 두 테마의 그림을 담고 색 42개와 이름을 같은 길이로 덮어쓰기), `docs/customkey.js`(일부러 공개한 커스텀 전용 열쇠). 템플릿은 `mix.py --template` 이 표식 색 `#FFA5xxxx` 과 64바이트 이름 자리로 구우며 패키지는 `custom`, minSdk 는 24 다. 테마별 안드로이드 색 42개는 `docs/acolors.json`(93KB, 안드로이드에서만 받아 간다). `gen.android_colors` 가 색 목록을 내는 유일한 자리이고 292벌 `colors.xml` 을 대조해 값이 그대로인 것을 확인했다. `node tools/check-apkmix.mjs --install` 이 apksigner 검증 + 안드로이드 14 에뮬레이터 설치까지 본다 — 통과했고 `aapt2` 로 색 42개와 바뀐 테마 이름도 읽힌다. 말풍선 색 고르기는 안드로이드에서 숨겼다(9-patch 의 `npTc` 조각이 캔버스를 거치면 날아간다).
- **0.39 를 냈다**(자산 585개 — `.ktheme` 292 + `.apk` 293. 늘어난 하나가 `custom-template.apk` 다). 안드로이드 커스텀 테마가 폰 브라우저에서 바로 만들어진다. 빌드 7분 54초, 실패 0. 그림 29,571장을 0.38 과 해시로 대조해 **달라진 것 0** 을 확인했다(gen.py 의 색 목록 리팩터링이 기존 테마를 안 건드렸다는 뜻이다). 사이트 갱신 뒤 **사이트에 올라간 파일만으로** 섞어서 안드로이드 14 에뮬레이터에 깔아 봤다 — `Success`, `versionName=0.39`, `apkSigningVersion=2`, 테마 이름 `캠핑 어두움 이중관`(뒤에 자리 채운 공백까지 설계대로). `pages.yml` 이 릴리스에서 `.apk` 도 받아 `files/` 에 두고 배경 없는 테마 것은 버린다(확인: `glass-light.apk` 404). README 의 아이폰/안드로이드 두 절을 `커스텀 테마 제작` 하나로 합쳤고 프로필 README 도 고쳤다.
- 여기까지 됨(2026-09-18 뒤): 빌드 비용을 재고 제일 비싼 것을 고쳤다. **색 하나 고쳐 보는 한 바퀴(`preview.ps1`)가 7분 35초였다** — 릴리스 클린 빌드(7분 24초)보다 길었다. `gen.py` 에 테마를 고르는 인자가 없어 한 벌을 고쳐도 292벌을 다 그리고 미리보기까지 다시 만들었기 때문이다. 조각을 주면 그 몇 벌만 그리게 했다(`preview.ps1 neonlounge` 41초, **11배**). 페이지에는 늘 292벌이 다 담긴다 — `preview.generate(ts, only=)` 의 두 인자가 그 구분이고 `tools/check-pick.py` 가 본다. 선택 빌드가 전체 빌드와 **바이트까지 같은 것**을 확인했고(neonlounge 4벌 × 109파일), `index.html`·README·카테고리 문서도 전체로 돌린 것과 차이가 없다. 측정은 `.claude/build-report.md` 에 있다: 클린 빌드 7분 24초 중 패키징 61%·소스 생성 37%, 패키징은 aapt2 의 PNG 재압축이 지배(`--no-crunch` 면 83배 빠르지만 산출물이 최대 33% 커진다), 소스 생성은 같은 배경을 세 크기로 다시 그리는 것(`chat_bg` 테마당 10회)과 `randint` 1,826만 회가 먹는다. 이 셋은 **릴리스 빌드만** 줄이므로 손대지 않았다 — `randint` 는 바꾸면 기존 292벌의 그림이 달라진다. 이어받은 `.claude/handoff.md` 는 C++ 프로젝트를 전제로 쓰여 있었다(이 저장소는 C/C++ 파일이 0개다). 헤더·include 사슬이 없으니 3단계(빌드 맵)와 4단계(git hook)는 버렸고 그 판단을 보고서에 적었다.
- 여기까지 됨(2026-09-18 뒤): **0.39.1 을 냈다.** 안드로이드에서 카톡 인앱 브라우저로 커스텀 테마를 만들면 페이지는 「받았음」 이라 적는데 카톡은 「지원하지 않는 파일 형식입니다」 만 띄우고 파일이 안 생겼다. 원인은 **안드로이드 WebView 가 `blob:` 주소를 못 내려받는 것** — 주소가 그대로 앱의 DownloadListener 로 넘어가는데 앱에는 그걸 풀 길이 없다. `a.click()` 이 던지지 않으니 페이지는 성공으로 알았다. APK 자체는 멀쩡했다(사용자가 고른 조합 그대로 `check-apkmix.mjs` 통과). `make.html` 의 `if (android)` 가 먼저 걸려 원래 있던 카톡 경고도 안 떴다 — 이제 UA 의 `; wv)` 로 인앱 브라우저를 가려내(`inApp`) 만들기 전에 막고 다른 브라우저로 보낸다. 카톡뿐 아니라 네이버·인스타도 같이 걸린다. 릴리스 8분 30초(소스 생성 2분 43초 · 패키징 4분 14초 · 태그+자산 585개 1분 33초), 실패 0, Pages 배포까지 확인했다.
- 첫 릴리스 시도는 **다른 세션이 같은 저장소에서 `gen.py` 를 돌려 296벌이 깨졌다**. 저쪽이 `build-src/` 를 지우고 다시 만드는 사이 내 패키징이 사라진 파일을 읽었다. `release.ps1` 이 EXIT 1 로 멈춰 태그도 자산도 안 나갔다. 프로세스 사슬(`Win32_Process` 의 ParentProcessId)을 따라가면 어느 세션이 띄운 것인지 나온다. 브랜치만 나누는 것으로는 안 막힌다 — 충돌하는 곳이 git 이 아니라 `build-src/`·`dist/`·`assets/` 다.
- 여기까지 됨(2026-09-18 뒤): **`build-src/`·`dist/`·`assets/` 를 두 세션이 같이 만드는 것을 막았다**(`tools/lock.py`). 0.39.1 첫 릴리스에서 296벌이 깨진 것과 미리보기 `.webp` 여섯 장이 달라 보인 것이 같은 사고였다 — 브랜치로는 안 막힌다는 진단이 맞았고, worktree 도 이 셋은 못 가른다. 락 파일은 `%TEMP%` 에 둔다(저장소 안에 두면 `release.ps1` 이 멈춘다). 쥔 채로 죽었으면 뺏고, `KTHEME_BUILD_LOCK` 으로 `release.ps1`→`build.ps1`→`gen.py` 사슬에서 한 번만 잡는다. `python tools/check-lock.py` 가 보고, 실제로 두 빌드를 겹쳐 띄워 두 번째가 멈추는 것을 확인했다. `docs/themes.json` 의 `version` 을 뺐다 — 읽는 곳이 어디에도 없는데 미리보기를 돌릴 때마다 그 한 줄로 워킹 트리가 더러워져 릴리스가 멈출 수 있었다(커밋된 값은 0.38 로 두 릴리스째 낡아 있었다). 릴리스 시간 배분은 8분 30초 = 소스 생성 2분 43초 + 패키징 4분 14초 + 태그·자산 1분 33초라, 업로드보다 패키징이 크다. 남은 후보(aapt2 `--no-crunch` 2분, 배경 캐시, `randint`)는 전부 릴리스 빌드만 줄이므로 손대지 않았다 — `--no-crunch` 는 배포 파일이 최대 33% 커지는 거래라 Pillow 쪽 팔레트 PNG 가 제대로 된 길이고, 그건 기존 292벌 그림을 바꾼다.
- 여기까지 됨(2026-09-18 뒤): **릴리스 빌드를 7분 24초에서 5분 34초로 줄였다.** `aapt2 compile` 결과를 `res/` 해시로 캐시한다 — 이 단계가 패키징의 대부분인데(PNG 재압축) 입력이 `res/` 뿐이고 버전은 `AndroidManifest.xml` 에만 있어서, 버전만 올리는 릴리스는 292벌이 전부 걸린다. APK 293개를 두 빌드에서 대조해 바이트까지 같은 것을 확인했다(`build-src` 를 다시 만든 뒤에도). 키는 `gen.py` 가 적는다(`res.sha256`) — PowerShell 에서 계산했더니 빌드가 10분 47초로 **느려졌다**. `--no-crunch` 는 쓰지 않았다(APK 가 최대 33% 커진다). 남은 자리는 `apksigner` JVM 220초 CPU 인데 배치 모드가 없다. `ZipFile.CreateEntry` 가 만든 시각을 박아 `.ktheme` 이 매번 달라지던 것도 고쳤다 — 한동안 「미리보기 비결정성」 으로 오해했던 것이 이 자리다. **헛다리 둘**: 워커를 8→12 로 올린 것은 4% 뿐이었고(Ryzen 5 5600X 가 물리 6코어 + SMT 라 8 이미 물리 코어를 넘었다), `randint` 를 `_randbelow` 로 바꾼 것은 벽시계에 안 나타나 되돌렸다(뜨거운 두 벌의 프로파일 비중을 292벌에 그대로 적용한 것이 잘못이었다). `tools/check-cache.py` 를 더했다.
- 다음 할 것: **`docs/make.html` 을 폰에서 실제로 열어 본다 — 만들어 본 적이 없다.** 볼 것 셋: 미리보기가 폰 화면과 같은지, 공유 시트로 카톡에 보낸 `.ktheme` 가 설치되는지, 1.7MB 짜리 테마 둘 받는 동안 기다릴 만한지. **`커스텀 테마 제작` 워크플로도 아직 CI 에서 돌린 적이 없다** — 러너에 `ANDROID_HOME` 과 `keytool` 이 있다고 보고 짰다. **안드로이드는 에뮬레이터로만 확인했다. 진짜 폰에서는 아직 안 깔아 봤다** — 에뮬레이터가 PackageManager 검증을 그대로 도는 대상이라 거의 같겠지만, 받는 흐름(크롬이 `.apk` 를 받고 「출처를 모르는 앱」 허용을 묻는 자리)은 폰에서만 보인다. **이슈 폼도 화면으로 본 적이 없다** — 로그인해야 열려서 여기서 확인이 안 됐다. 값이 채워진 링크를 한 번 열어 칸 다섯이 다 차 있는지, `Create` 하면 워크플로가 걸리고 댓글이 달리는지 본다. 그 다음은 v4·v5 와 0.36 의 32벌, 0.38 의 새 36벌을 폰에서 보기 — 미리보기로만 봤다. SPDX 헤더는 새로 만든 `tools/zoo.py` 에만 있다 — 나머지 `tools/*.py` 에 한 번에 넣는다. 실행화면 디자인은 사용자가 보류했다.
- 막힌 것: **인앱 브라우저에서 만든 테마를 바로 까는 길은 없다** — 서비스 워커로 가짜 `.ktheme` 주소를 흉내 내 폰에서 찍어 보니 404 였다. 카톡은 그 주소를 웹뷰가 아니라 앱에서 직접 받는다. 실험 파일은 지웠고 결론은 CLAUDE.md 에 적었다. 만든 테마는 늘 공유 시트로 채팅방을 한 번 거친다. 테마 규격에 글꼴 자리가 없어 말풍선 글자는 늘 기본 글꼴이다. worktree 에는 `theme.keystore` 가 없어서 거기서 빌드하면 새 키가 만들어진다 — 릴리스는 원래 폴더에서 하거나 키를 복사한다. 글자 단위로 합친 README·카테고리 문서는 칸이 밀릴 수 있어 합친 뒤 미리보기를 다시 그린다. 사파리 한 번에 설치는 여전히 길이 없다. **미리보기 비결정성은 없었다.** 292벌을 통째로 다시 그려 커밋된 것과 대조했더니 `.webp` 한 장도 안 달라졌다(같은 프로세스 두 번·프로세스를 갈라 두 번·단일 렌더 40장도 전부 같다). `preview.py` 에는 난수 호출 자체가 없다. 그때 여섯 장이 달라 보인 것은 씨앗이 아니라 **다른 세션이 같은 시간에 `assets/` 를 같이 쓴 흔적**이다 — `build-src/` 에서 296벌이 깨진 것과 같은 사고다. 이제 `tools/lock.py` 가 막는다.
